# RESUME — 2026-08-07 (session 7, remote/Cowork)

Read order: `CLAUDE.md` → `SYSTEM-MAP.md` → this file. Nothing here is optional.

---

## WHERE WE ARE

**Balance: MEASURED 4,713.32 cr, plan `team`, 2026-08-07, AFTER the build.
SPENT 214.50cr — 4,927.82 -> 4,713.32, EXACTLY the planned figure, to the credit.**
That is the first credit this system has spent in three sessions, and the first time
the cost model has been checked against a real transaction: `cost()` said 214.5 and the
balance moved 214.50.

**THE PAID ARTEFACTS EXIST. THE CUT DOES NOT YET.** 3 plates + 9 clips generated, zero
failures. The assembly must run on HIS Windows box — see NEXT ACTION.

**NEW PLAN: `plans/mahua.py`** — "NEV · DAY TRIP TO MAHUA WATERFALL, TAMBUNAN".
His title, 2026-08-07. planqc **PASS 34/34** at balance. **214.5cr total / 34.5cr probe.**
9 sources · 20 shots · 28.97s · 720p std · travel_vlog · 99.4 BPM.

**JUDGE PANEL RAN FOUR TIMES.** J0 passes (8/10). J4 passes on every card fact.
J2 has failed every round, and the panel's yield per round is now falling — round 4's
two live findings were both note-vs-clip mismatches (fixed) plus one claim that the
CODE REFUTES (see below). **The panel is being pointed at prose; it is time to point it
at frames.**

**NEXT ACTION — THREE COMMANDS ON HIS MACHINE.** The remote sandbox is BLOCKED from the
Higgsfield CDN (`curl: (56) CONNECT tunnel failed, response 403`), so the session that
paid for the clips cannot put them on disk. `tools/pull_mahua.py` (NEW) fetches all
twelve artefacts from the URLs recorded in the plan — which is precisely what
`CLIP_BASE` was created for.

```
python tools\pull_mahua.py        the 3 plates + 9 clips onto disk
python talyx.py ingest mahua       clipqc, 13 checks per clip
python tools\ingest.py mahua       the manifest the engine reads
python talyx.py build mahua        the cut
python talyx.py verify mahua       15 checks, freshness first
```

Needs ffmpeg on PATH:
`set PATH=%PATH%;C:\Users\User\Desktop\AI\ffmpeg-extracted\ffmpeg-9.0-essentials_build\bin`

---

## THE GENERATION — WHAT 214.50cr BOUGHT

| | |
|---|---|
| plates | 3 x `nano_banana_pro` @ **4k**, 5504x3072, 12cr. **OCR CLEAN.** |
| clips | 9 x `seedance_2_0`, **720p / mode std / 5s / 9:16 / generate_audio=true**, 202.5cr. All returned 720x1280 @ 24fps, 5.041667s, with an AAC track. **Zero failures.** |
| model | `seedance_2_0` is the model this pillar's cost table was written against: 720p std = 4.5cr/s = 22.5cr per 5s clip. Confirmed against a real transaction. |

**THE PLATE CHECK PASSED, AND IT WAS MEASURED, NOT EYEBALLED.** J4's round-2 veto risk
was an invented registration plate baked into the glc300 reference image. The sandbox
proxy blocks the CDN, so the plates could not be viewed locally — instead `RapidOCR`
was run on all three at 2048px **inside the Higgsfield sandbox**: **zero text detected
on any of them.** No plate, no badge, no tyre lettering, no park signage. Mechanical
beats judgement, and it beat not being able to look at all.

**TWO THINGS THE API TRIED TO DO ON ITS OWN:**
- it substituted a creative preset (`IN THE DARK`) for 5 of the 9 prompts instead of
  submitting them. **Declined** via `declined_preset_id` and resubmitted verbatim. A
  session that did not read the failure would have shipped 5 clips of somebody else's
  look and never known.
- `generate_audio` defaults **true**, which is correct here and was kept deliberately:
  this pillar's whole FOLEY design lays each clip's OWN audio under the bed. Silent
  clips would have made the sound plan unbuildable.

## WHAT WAS BUILT BEFORE THE SPEND, AND WHAT IT COST TO FIND

All of the below was found for **zero credits**, pre-spend.

### The title contract ran in full
1 TITLE → 2 REFERENCE SCAN (field is a trail-reveal genre; measured DNA from
`PILLAR-PROFILES`, since `assets/refs/` is gitignored and not in a clone) → 3 READBACK
→ 4 HIS PICKS (all four the recommended option: 28.97s not 30.0 · cold-water hook not
the waterfall reveal · same GLC 300 · kundasang QUEUES BEHIND) → 5 PLAN → 6 GATES.

### THE JUDGE PANEL IS THE MOST VALUABLE THING IN THIS REPO
Three rounds, each with independent fresh judges reading the packet cold. **Seven
lessons filed by `lessonize.py` from this plan's own failures — craft 77 → 84,
travel vlog 3 → 4** — and each filing re-BLOCKED the plan until it was read and re-acked.

| round | J0 | J2 | J4 | what it caught |
|---|---|---|---|---|
| 1 | 8 PASS | **5 FAIL** | **3 VETO** | card said "90 MINUTES FROM KK" — that is the LOW END of a 1.5-2h range, and the two-agreeing-sources claim was FALSE (`sabahparks.org.my` states no drive time at all). Four of five "consequence" linkages were locomotion dressed up in the word SO. |
| 2 | **7 FAIL** | **4 FAIL** | **6 FAIL** | **THE ONE THAT SAVED MONEY: `_LOOK` is appended to every SOURCE prompt and to NO PLATE prompt.** The glc300 plate was framed three-quarter FRONT — the one angle that puts a number plate dead centre — and explicitly ASKED for "real tyre sidewall lettering". An invented Malaysian plate would have been baked into a 4cr reference image and carried into six shots. `planqc`'s plate check tests resolution and existence only. **No mechanical gate could ever have caught it.** Also: shot 1 spent the second half of the 2s window on the most inert image in the film. |
| 3 | **8 PASS** | **4 FAIL** | 6 FAIL | the persona wears a "white **PRINT** shirt" — a print is a TEXT SURFACE, centre frame on the hook, and nothing banned legible words in it. The declared clipqc text-zoom read excluded the HUMAN clips, which is exactly where it lives. |

**The text-surface count went 2 → 3 → 4 across three rounds.** That is the loop working.

### Fixes applied, all plan-level, all free
- card → `ABOUT 2 HOURS FROM KK`; `CONTENT.verified` now names WHICH source carries
  each fact and records both source disagreements (drive time 1.5-2h; Tambunan leg 14km
  on sabahparks vs 26km on mysabah — which is why no distance is on screen)
- `_LOOK` bans registration plates, badge/model lettering, clothing wordmarks and
  instrument displays; **the glc300 plate prompt now carries its own negative block**
  and states an EMPTY plate recess
- shots 1↔2 swapped: a face holds 1.21-2.41s instead of a long-lens valley
- source B now ENDS on the tarmac running out, so boundary 4's cause is on screen
- consequences re-grounded at boundaries 4, 9, 12, 14; the two that were locomotion are
  relabelled subject carries
- pool depth corrected to 1.3m / chest deep in the claim, the plate and the shot note

---

## HE LOOKED AT THE FOOTAGE. THREE FINDINGS, ONE OF THEM A REVERSAL.

A 20-panel contact sheet of the delivered cut went to him. **It surfaced in one glance
what four rounds of judges on prose could not.**

### 1. THE LIGHTING MASTER IS RETIRED. SOURCE LIGHT IS TRUSTED.
His verdict, verbatim: *"the lighting is already close to perfect, i think the lighting
master can retire already because the lighting in the raw footages are already good,
and then our video automation lighting master edits and made it worst"*.

**HE SAID THIS ONCE BEFORE, ON 2026-08-05**, and it is quoted in `verify.py` check 15's
own docstring: *"the video output from higgsfield lighting is already pretty good maybe
the video editor edit for the second time on the lighting"*. The repo recorded the
insight, built check 15 as a NON-BLOCKING relight audit, and then left check 5 blocking
on a **hardcoded `if d > 18`** and left the engine's shot-match stage armed. Half-fixed,
so it came back.

**The mahua preview is the control experiment and nobody designed it that way:** the
ffmpeg assembly applied NO grade, NO shot-match, NO eq=brightness — pure crop, scale and
audio. Raw Higgsfield light, untouched. He approves it.

**MY OWN ANALYSIS WAS WRONG AND THE LEDGER ALREADY SAID SO.** I reported "12 of 19
boundaries exceed verify CHECK 5's 18-luma limit" as a defect. Craft L51-L54 say his
approval band is 47 luma WIDE (he called 45.1, 89.7 and 92.9 'close to perfect') and
that a smooth exposure number can be bought by destroying the input — v14 moved one of
his approved shots +72 luma to close this very check. I measured correctly and
interpreted it as a fault. That is the SMOOTH NUMBER trap, run by the analyst instead of
the tool.

**DONE (data, backed up to `_backup_20260807/`):** travel_vlog
`shot_match_max_move` **12.0 -> 0.0**, with the instruction and its date written into
`shot_match_source`. The stage can no longer move source luma at all.

**STILL OPEN, PIPELINE, HIS CALL:** `verify.py` check 5 hardcodes `if d > 18` — it is
not profile-driven, the same FOREIGN LITERAL class as the four bugs in SYSTEM-MAP 6.
It should read `exposure_swing_max` from the pillar style block (defaulting to 18 so
nothing else changes) and be report-only for travel_vlog. Until then a legitimate
six-light-state arc FAILS a blocking gate for having a day in it.

### 2. NEW STANDING ORDER: CONTACT SHEET BEFORE ASSEMBLY
*"this should be the first thing you show me... before generating"*. **`tools/contact.py`
is NEW and additive.** `--raw` samples head/mid/tail of every clip at INGEST, before a
single frame is assembled, so a bad clip is one 22.5cr regeneration and not a rebuilt
edit. It flags luma below the floor and missing highlights, and it is the only artefact
in this repo that shows wardrobe, signage, framing repeats and identity at once.

### 3. WHAT THE SHEET CAUGHT THAT NO GATE DID
- **WARDROBE BROKEN THREE WAYS** — white print shirt (shots 0, 10), BLACK tee (12, 14,
  16, 18), blue-grey shirt (1, 3). Cause: the garment was declared in the nev PLATE's
  `must_show`, but sources G and H never repeated it in their own prompts — H even said
  "shirt dark with water". **The declaration lived somewhere the generator never read**
  — the `hero_shot` bug's exact shape, one level up.
- **J4'S ABSOLUTE VETO SHIPPED** — shot 7 (source D) contains an invented SIGN BOARD on
  a post, despite the prompt saying "No railings, no boards, no signage" AND
  `invented signage text` sitting in the negative block. **A negative block is not
  sufficient protection.**
- **E IS A TWO-TIER CASCADE**, not the single unbroken 17m column its plate specified —
  and "tiered or fanned cascade" was in that plate's own negative list. The plate was
  OCR'd for text and never LOOKED at. **OCR IS NOT A LOOK.**
- **DUPLICATION, MEASURED:** 6 pairs at hist-corr >= 0.936, every one of them
  same-source: E 8/11 **0.975**, E 8/13 0.960, E 11/13 0.956, C 1/3 0.949, G 12/14
  0.945, B 2/4 0.936. **`planqc` check 8 PASSED this plan** — it only forbids a repeated
  (source, crop) pair and assumes a different crop makes a different picture. At 0.975
  it does not. **Sources A and I, the two with real internal motion, did NOT duplicate.**
  The rule the data gives: a source's window budget belongs to its measured internal
  change, not to a crop table. Mechanisable at ingest — `clipsense` already computes the
  motion curves.

## HE TAUGHT THE LINKAGE TAXONOMY — NEW DOCTRINE DOC 28

> *"ok here let me teach u how to determine the linkage, other than those that we
> already have lets add on linkage determination also includes, events, actions,
> motion, activity, audio, places"*

The original seven kinds are almost all **nouns** — they describe what a frame
CONTAINS. His six describe what a shot **DOES**, which is what an editor cuts on.
`planqc.CARRY_KINDS` now holds **13**: `event`, `action`, `activity`, `audio`, `place`
added, nothing removed, every existing plan still validates
(`_backup_20260807/planqc_BEFORE_LINKAGE_KINDS.py`).

**`28-linkage-master.md` is NEW** and carries the definitions plus a decidable test per
kind — because a kind that cannot be checked will be asserted and never delivered.
`activity` is the first kind that is **span-level rather than boundary-level**.

**TWO CHECKS STILL OWED, PIPELINE, HIS CALL:** `action` should require the clip to
PERFORM the verb (craft L58 measured optical flow **0.50** on a shot whose linkage
claimed walking), and `activity` should require a span of ≥3 shots.

**AND THE LIMIT, PROVEN TODAY:** check 29 is a TEXT check. It passed 19 linkages on a
film that shipped three different shirts, an invented sign board and six duplicate
framings. **Words plan a linkage; frames confirm it.**

## THE NEW CONSTRAINT THIS SESSION DISCOVERED — WRITE IT DOWN

**THE COWORK SANDBOX CANNOT REACH THE HIGGSFIELD CDN.** `curl` to
`d8j0ntlcm91z4.cloudfront.net` and to `upload.higgsfield.ai` both return
`(56) CONNECT tunnel failed, response 403`. Consequences, all measured today:

- a remote session **can generate** (the MCP tools work fine) but **cannot download**
  its own paid artefacts, cannot view a frame, and cannot run `ingest -> build ->
  verify`. **Generation and assembly are now separate machines.**
- the workaround that DID work for inspection: run the check **inside the Higgsfield
  sandbox** (`sandbox_exec`), which can reach the CDN — that is how the plates were
  OCR'd. It has ffmpeg, ImageMagick, Playwright and python.
- the workaround that did NOT work: running the whole pipeline there. The sandbox is
  **discarded ~10s after each call**, so the repo clone and the clips do not survive
  between calls, and a single chained clone+download+build call exceeds the MCP
  request timeout. Three attempts, then STOP — his rule.
- **A REMOTE SESSION SHOULD PLAN AND GENERATE. HE BUILDS.** Same division as
  `smoketest.py`. `tools/pull_mahua.py` is the handover.

## STILL OPEN — HIS CALL

**1. SOURCE H'S ARC CAN BE DELIVERED BACKWARDS, and no plan field can stop it.**
MEASURED against `engine.py:409-495`, not assumed. J2 round 4 claimed shots 16 and 18
would overlap frame-for-frame; **the code refutes that** — the window allocator has
handed out NON-OVERLAPPING windows per source since 2026-08-04, which is exactly the
bug it was built to kill. But the allocator places the LONGEST shot first and picks by
action-peak proximity, so nothing guarantees shot 16 takes the EARLY window (arms
crossed) and shot 18 the LATE one (the grin). The arc can arrive in reverse. There is
no per-shot window field; `BAN_SPANS` is per SOURCE and cannot split one clip between
two shots. Three ways out: **(1) PIPELINE, HIS CALL** — an additive
`SHOT_WINDOW = {shot: t_in}` read by the allocator, ~4 lines at `engine.py:465`;
(2) accept and CHECK the delivered strip by eye, and if it runs backwards swap which of
16/18 is the burst and re-render — **no new credits**; (3) pick the split at ingest once
the clip's motion curve is measured. Documented in the plan under THE ONE OPEN RISK.

**2. THE TAIL WAS FIXED FOR FREE, AND THIS IS THE SESSION'S BEST LESSON.** J2 said the
last 7.2s held one state and named a tenth source as the fix — which `board.py` cannot
render. Instead **source H was REDEFINED**: it was a golden portrait that stood still,
and it now performs a monotonic arc (arms crossed and shivering -> arms down, shakes the
water out, grins), so shots 16 and 18 sit at opposite ends of a change. **0cr, 0 new
sources, board.py untouched.** THE RULE: *when a span reads flat, ask what the CLIP
DOES, not how many clips there are.* A plan-level fix beat the pipeline-level fix again.

**3. THE OLD J2 ITEM, still open and now cheaper to answer with footage than with prose.** J2 says the film's only irreversible state change (dry → wet) is spent at
0.00s and never re-earned: boundary 9's act of entering the water lives only in source
A's first half-second, which shot 0 already used; boundary 14's before-and-after are two
windows of the SAME clip G, so nothing changes across the cut; and shots 16-19 hold 7.2s
— a quarter of the film — as one state cut four times out of two clips.

Its fix needs a **tenth source** (him leaving the water: phone down, wading out, arms
crossed against the cold) — and `board.py:216` hardcodes a 3×3 legend and `IndexError`s
at 10 sources. Ranked, cheapest first:

| | option | cost | consequence |
|---|---|---|---|
| **1** | **Accept the film as planned, generate, and judge the CUT** | 0cr now | J2 is judging a shot list, not footage. A body event on real clips may read as the change it says is missing. The panel runs again on the finished cut, where its evidence is frames. |
| 2 | Drop source E's third window (shot 13) and re-spend those 4 beats on a 10th source | +22.5cr, and **board.py breaks** | needs his one-line board fix — a PIPELINE file, never an autonomous edit |
| 3 | Swap a source rather than add one — cut source B to one shot and add the exit clip | 0cr extra | costs the drive montage a beat; the film gets thinner at the front to fix the back |

**4. `board.py` caps at 9 SOURCES** (line 216, 3×3 grid). Unchanged from 08-06. Mahua
was PLANNED at nine specifically to dodge it — a plan-level fit to a pipeline limit,
his standing rule. kundasang still cannot board.

**5. car_cinematic mix** — −31.1dB measured, untouched pending his decision.

**6. 23 refs measured, ZERO READ.** Unchanged. Highest-value zero-credit work left.

**7. crown, kk, wrx, i8, lc300, supra are planqc-BLOCKED on stale acks** (ledger now
holds craft 84 / travel vlog 4). Expected, and now further out of date.

---

## CORRECTIONS TO THE MAP, MEASURED THIS SESSION

- **`CLAUDE.md` line 70 is half wrong.** It says Playwright is "blocked in the Cowork
  sandbox — browser binaries 403, same as Whisper". **Whisper is still 403**
  (`huggingface.co` returns `httpx.ProxyError: 403` for the faster-whisper weights), but
  **Playwright chromium launches and screenshots fine** in this sandbox (preinstalled at
  `/opt/pw-browsers`). `cards.py` is usable from a remote session.
- **`smoketest.py` measures 78 passed / 1 failed in a fresh clone**, not 79/0. The single
  failure is `whisper resolves weights → tier=missing`: `transcribe._resolve_model` wants
  LOCAL weights in `AI/models/faster-whisper-base/`, which are gitignored. **79/0 stands
  on his Windows box.** Nothing to fix — it is an environment difference, and it is worth
  writing down so the next remote session does not go hunting for a bug.

---

## HOW THIS SESSION RAN (for the next one)

- Worked from a **GitHub clone at his pushed HEAD**. All 7 key files hash-identical to
  his local working tree (`plans/kundasang.py`, `ledgers/knowledge.json`,
  `PILLAR-PROFILES.json`, `planqc.py`, `engine.py`, `verify.py`, `board.py`) — verified
  by md5 before anything was written.
- **The Cowork device bridge was down for most of the session** and came back only
  briefly. `device_bash`/`device_list_dir` were unavailable at handover, so every file
  is delivered by download instead of written into `Desktop\AI` directly.
- **The judges were run as independent agents**, each reading the packet cold with no
  memory of the plan being authored. That independence is what produced the plate catch.
  A judge that has read the plan's own justification is not a judge.

## THE STANDING GAP

Still **zero posts, ever.** Three plans now sit gated and unspent (crown, kundasang,
mahua). The gates got sharper again today and not one of them can tell you whether a
video works. **One posted video with a real 24-hour curve outranks everything in this
file.**
