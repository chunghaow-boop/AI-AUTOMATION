PASTE THIS INTO A NEW CHAT
==========================

Read these in order, then tell me where we are in ONE line and wait:
    1. CLAUDE.md
    2. SYSTEM-MAP.md
    3. RESUME-2026-08-12b.md          <- session 10. THE newest. Read all of it.
    4. 28-shortdrama-scriptwriter.md  <- new SCRIPTWRITER seat (if planning)

FIRST TWO THINGS, ALWAYS:
  1. MEASURE the credit balance (was 3,977.57 team, 2026-08-12).
  2. git -C <repo> --no-optional-locks log --oneline -1
     Expect 00cc146 OR NEWER (he pushes with PUSH.bat). If it is e2b4485 the
     session-10 direct writes were never committed - STOP and tell him.

RULES THAT BITE (full text in knowledge.json, craft 133 / travel vlog 8):
  - L130 re-measure every "done" - tools lie, humans repeat them.
  - L133 bridge git is read-only + --no-optional-locks. No stash/merge/fetch on the mount.
  - L132 one truth channel per session. Session 10 used DIRECT WRITES on top of 00cc146.
  - L125 whole clips, reorder only. L128 travel_vlog bed OVER foley (bedcheck.py gates it).

WHERE WE ARE (2026-08-12b):
  panborneo V5 remix queued on HIS BOX (command in the RESUME, bedcheck must PASS).
  Then: publish (AI label + compression caption + LiQWYD credit). ZERO POSTS, STILL.
