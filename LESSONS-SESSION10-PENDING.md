# MERGED 2026-08-12 — this file is now a stub, park in _to_delete\ at will.
L128 → ledgers/knowledge.json topic "travel vlog" (count now 8).
L129–L134 → topic "general craft" (count now 133).
Backup of the pre-merge ledger: _backup_lessonize/knowledge_pre-session10.json
Every plan acked below craft 133 / tv 8 is now planqc-23-BLOCKED until it
reads the new lessons (lessonize --brief) and re-acks. That is the loop working.
