# RESUME — 2026-08-12c (session 11, Cowork) — V6 tooling built + gated, render pending on his box

Read order: CLAUDE.md → SYSTEM-MAP.md → this.

## THE ONE-LINE ANSWER HE EXPECTS
**His two V5 catches (basic/clashing captions, missing transitions) are FIXED IN
TOOLING and proven on a synthetic build; PANBORNEO_V6 needs ONE command on his box
(clips absent from mount). V5 remains approved-but-unposted. Balance 3,764.14cr
(team) measured at session start — 213.43cr spent OUTSIDE our sessions, unexplained;
ask him. Zero spent this session.**

## HIS DECISIONS THIS SESSION (recorded)
- ACE Studio MCP: **DROPPED** (paid). Remove from next-actions.
- V6 FULL before posting (captions + transitions), over post-V5-now. His call,
  against the standing-gap default — noted, not relitigated.
- Caption style: web reference scan (2026 field standard: phrase captions, ONE
  accent keyword #FFD54A-class, scrim, 100-150ms pop-in) → pill + white + yellow
  keyword, y=0.70. He saw the before/after composite on the SABAH clash frame.
- **720p LOCKED, no upscale, no 1080p** — his explicit call 2026-08-12: 720p IS the
  cost strategy (4.5cr/s vs 9). The measured verdict stands: encode chain clean,
  softness is display upscaling, platforms recompress anyway. Do not re-raise.

## BUILT + VALIDATED THIS SESSION (all additive, zero credits)
1. `tools/capcheck.py` — caption-legibility gate (L137). Band-median luma per span,
   worst frame governs, WCAG floor 4.5:1. **Calibrated per L136: fails V5 at 4.29:1
   on exactly the card his eye flagged (SABAH on bright road), passes the other 4.**
   `python3 tools/capcheck.py VIDEO --cards X_cards.json` (or `--v5`).
2. `tools/capcards.py` — PIL card renderer for the captionmgr seat. Real per-word
   keyword highlight (drawtext never could), pill scrim black@0.45, CapCutSansText-Bold,
   120ms fade-in, real-metric width fit (L138 — the 0.52em estimate shipped a 784px
   card onto a 720px frame; caught, fixed, re-run green). captionmgr still PLANS
   (grouping/emphasis/overflow); capcards RENDERS. Playwright stays the premium path.
3. `tools/build_panborneo_v6.py` — v4 grid byte-identical + capcards overlays replace
   drawtext + 4 motivated transitions at story turns, ALL zero-drift (L139):
   b1 G→B flash-white 3f (REWIND) · b4 whip slideleft unchanged · b7 E→I dip 4f
   (state line) · b11 K→L dip 4f (arrival). Emits `<out>_cards.json` for capcheck.
4. **Synthetic proof-build passed every check**: 1536 frames EXACT with transitions
   active (no DESAFARM drift), flash mid=255 luma, dip mid=29, capcheck 5/5 PASS
   (worst 9.30:1). Filtergraph proven executable end-to-end.
5. V5 quality question answered by measurement: encode chain CLEAN (7.0Mbps CRF16
   720p, finalmix `-c:v copy`, zero generational loss). Softness = 720p source
   upscaled by the display. Resolution decision: see 720p LOCKED above.
6. Lessons L137/L138/L139 appended to general craft (now **138**; travel vlog 8,
   approvals 4). Backup: `_backup_20260812_prefinish/knowledge_pre-session11.json`.
   planqc 23 will demand re-ack at 138 — correct.

## ADDED LATER IN SESSION 11 — SHORT-DRAMA GRAMMAR NOW IN THE PLANNER (his order)
He asked for the K/J short-drama knowledge to be IMPLEMENTED, not documented.
Done, zero credits:
- **planqc 35 TURNS** — plans must declare `TURNS = [(t_s, "what changes"), ...]`;
  floor ceil(total/12), no gap >12s. Act labels rejected as the signal (framing
  notes → vacuous count, the L136 trap, avoided at design time).
- **planqc 36 TWIST TIMING** — `CONTENT["twist_at"]` seconds, ≤40% of runtime.
- Calibrated 3-way on wrx: FAIL undeclared / PASS declared / FAIL breaches with
  exact numbers. Full suite smoke-tested (`--plan wrx` runs, legacy blocks stand).
- File 31 doc updated: candidates section now records the implementation.
- NEW-PLAN CONTRACT ADDITION: every plan from now on carries TURNS + twist_at,
  written by the SCRIPTWRITER seat at step 5; seat judges turn REALITY, planqc
  counts and spaces them.
- His standing order this session: **NO CREDIT SPEND** until he gives the next
  title and passes the gate. 720p locked (see above).

## SESSION 11, LATER STILL — PIPELINE ORDER + TRANSITION LANGUAGE (his calls)
- **Pipeline order LOCKED** (docs/REMOTION-MOGRAPH-SPEC.md): rough cut (ffmpeg) →
  SOUND (finalmix) → PICTURE POLISH (Remotion, LAST — the layer his eye re-iterates
  most re-renders cheapest) → gates on final file only. Hard rules: frame count
  in==out; polish passes audio -c:a copy byte-identical; bedcheck re-runs at the
  end to PROVE the mix survived. Remotion PoC still pending on his box; until then
  capcards static PNGs inside the ffmpeg step fill the polish slot (V6 form).
- **His catch: "mostly whoosh" → L140.** BLEND_KIND was one string per film -
  per-scene choice was inexpressible. Now: TRANSITIONS.json has when_scene +
  executed_by + 2 polish-layer kinds (wipe, light_leak; 7 total); NEW PLAN FIELD
  `TRANSITIONS_PLAN = {after_shot: {kind, why}}`; **planqc 37** enforces: kind in
  bank, WHY names the scene change, sits ≤1.5s from a declared TURN, caps, variety
  (monoculture refused), each kind's OWN sfx bucket (whoosh-on-everything refused).
  Calibrated 4-way. Legacy BLEND_KIND path untouched (check 34 still owns it).
- Ledger: general craft now **139** (L140 added; count for planqc 23 ack = 139).
- **Bank EXPANDED to 12 kinds** (his ask: more scene/scenario coverage), with a
  `status` discipline: live (hard, whip, flash, dip, match_cut — proven executors)
  vs needs_poc (wipe, light_leak, glitch, shake_impact ← Remotion PoC unlocks these
  4; speed_ramp, zoom_punch ← engine tail-patch PoC; object_wipe ← polish +
  generation-side staged crossing). planqc 37 BLOCKS any plan declaring a
  needs_poc kind — an option without an executor is fiction (L140 inverse).
  Each entry carries when_scene + its own sfx bucket + caps (zoom_punch inherits
  the 1.4x punch cap — the 82% sharpness lesson).

## NEXT ACTIONS, IN ORDER
1. HIS BOX (clips live in Downloads):
   `python tools\build_panborneo_v6.py --clips C:\Users\User\Downloads --out panborneo_v6_diegetic.mp4`
   then finalmix, SAME L128 flags as V5:
   `python tools\finalmix.py --video panborneo_v6_diegetic.mp4 --bed assets\bank\bgm\travel_vlog\liqwyd-to-the-moon.mp3 --bed-ss 9.57 --whoosh assets\bank\sfx\whoosh_whip.wav --whoosh-at 19.37 --whoosh-gain 4 --foley-target -22 --bed-under -4 --duck-ratio 2 --duck-release 300 --out PANBORNEO_V6.mp4`
2. GATES on V6: `python tools\bedcheck.py PANBORNEO_V6.mp4` +
   `python tools\capcheck.py PANBORNEO_V6.mp4 --cards panborneo_v6_diegetic_cards.json`
   + verify.py + HIS EYE on transitions/captions (APV-4 covered the mix; same flags
   should reproduce, but re-measure — L130).
3. PUBLISH V6 (J4-binding, unchanged): AI label · compression caption (Brunei transit
   168km, 1,000+km, multi-day) · LiQWYD credit verbatim. **STILL ZERO POSTS — this
   outranks everything below it.**
4. Ask him: the unexplained 213.43cr spend since last measure.

## OPEN / PARKED
- Montserrat vs CapCutSansText on cards: capcards uses CapCutSansText-Bold (repo
  font, always present). His eye rules after V6 renders.
- Generation-side linkage (exit→entry motion INTO prompts) = the real transition
  fix, next NEW build (costs credits). planqc 24 already demands the intents.
- Remotion mograph PoC (docs/REMOTION-MOGRAPH-SPEC.md) — parked on his box.
- 6 stale pre-finish stashes — his call, `git stash list` then clear.

## NIAH — HE SAID GO. PHASE 2 RAN AUTONOMOUS (his test: "do not ask me anything")
- plans/niah.py: ALL 42 planqc checks PASS. 17 shots / 27.08s / 10 sources / 720p std.
- STORYBOARD (tools/storyboard.py, L141) reviewed BY HIM → GO given.
- **GENERATION (all job ids + QC numbers in projects/niah/JOBS.json):**
  - niah_mouth plate 4k: OCR 0 ✓ (dark-quartile 1.4 = crushed floor, WATCH)
  - niah_interior plate 4k: OCR 0 ✓, 14k ceiling glints ✓ (same dark WATCH)
  - **PROBE G PASS by measurement**: motion never settles (min 3.59), sky detail
    RISES 79→583 (distinct wingbeats, not smoke), audio −23dBFS, OCR 0.
  - **BATCH COMPLETE: 10/10 clips, ZERO failures.** All 720x1280/5.04s/audio.
    OCR sweep clean (H: single-frame transient glyph = noise, 4 frames clean).
    Luma arc tracks the story: day ~103-107 → cave 17-49 → dusk 58-82.
    **E twist verified by GLINT COUNT 53→172 rising** (laplacian fell — the beam
    pool dominates that metric early; glints are the honest signal on a dark
    ceiling. Lesson-grade nuance, fold into next lessonize). E stir window
    re-pinned 2.60→3.40 in the plan.
    (5 submissions needed declined_preset_id retries — Higgsfield now pushes
    preset recommendations on batch; retry with the returned id, costs nothing.)
  - **MEASURED SPEND: 233.00cr EXACT** (3,597.61 → 3,364.61) = the gated figure
    to the credit. Gallery shown in chat (12 items).
- QC method this session: **Higgsfield sandbox_exec** (curl + ffprobe + numpy +
  RapidOCR server-side) — the container cannot reach the hf CDN, the sandbox can.
  This is the remote LOOK protocol now.
- Balance at Phase-2 start: 3,597.61. THREE unexplained outside spends today:
  213.43 + 112.08 + 52.45 — ASK HIM.
- **PHASE 3 COMPLETE — NIAH_V1.mp4 DELIVERED, repo root + projects/niah/.**
  His call mid-session: "build a bridge" → **L142 THE CHROME BRIDGE** (browser
  fetches CDN assets one-per-page into C:\Users\User\Downloads, mounted on
  request) → full LOCAL pipeline same session, zero manual steps:
  build_niah_v1.py (plan-as-data, capcards captions, L139 fades, window CLAMP
  after the L143 catch: 3 med windows overran 5.04s sources, audio 2.04s short,
  NaN median - windows now obey win+dur+tail<=clip_dur) → bed liqwyd-to-the-moon
  (native 97.5 = the grid; window scan picked 105.0s start, energy lifts into
  the exodus) → finalmix L128 flags (+ strict-ffmpeg aformat pin added after
  loudnorm, no-op on his box) → GATES ALL PASS: bedcheck +1.7dB bed-over-foley /
  capcheck 5/5 (worst 11.7:1) / 812 frames exact / I -11.3 LUFS (V5's approved
  class) → STORYBOARD now 17/17 REAL FRAMES, 0 missing → mastermind LOOK: twist
  ceiling + the grin frame both read; identity = Nev (face, earring, wardrobe).
  Ledger: craft 142 (L141 storyboard, L142 bridge, L143 window arithmetic).
- AT PUBLISH (J4-binding, unchanged): platform AI label · LiQWYD credit verbatim
  for to-the-moon · caption notes the one-visit compression. HIS EYE first.
- **HIS VERDICT ON V1, AND THE V2 REBUILD (end of session):** "the footage are
  good, it's just that the video editor cut the raw footage already." V1 chopped
  5.04s sources into 1.23s bursts, each source twice, to hit the 30s title spec.
  **NIAH_V2.mp4 = WHOLE CLIPS, REORDER ONLY**: 10 clips x 8 beats, t_in=0, one
  appearance each, 49.23s, order as the only edit decision - and that order fixed
  V1's ending defect for free (ends on dusk ribbons, not C's bright daylight
  cliff). Gates: bedcheck PASS +0.5dB · capcheck 5/5 · 1477 frames exact ·
  -10.6 LUFS. Bed re-scanned -> 90.0s window.
- **THE RULE IS NOW IN THE SYSTEM, NOT BESIDE IT** (his order: adjust files,
  build nothing new):
  · `CLAUDE.md` HARD RULE 0 - whole clips, the edit is the order, duration is a
    request and the rule is an order.
  · `planqc.py` **check 38 WHOLE CLIP** (blocking) - any shot shorter than its
    clip needs CUT_JUSTIFICATION, any source reused needs REUSE_JUSTIFICATION.
    Checks 2 (pillar band) and 9 (hook <=2s) now REPORT instead of block on a
    whole-clip film; check 21's 0.1s shave no longer makes a whole-clip plan fail
    against itself. All calibrated: niah PASSES all 43.
  · `plans/niah.py` rewritten to the delivered 10-shot whole-clip shape (V1 shape
    archived in-file as comments), lessons re-acked 144, LINKAGE/duck list/hero
    shot re-derived for the new order.
  · `tools/build_niah_v2.py` -> `tools/build_niah.py`; v1 builder parked in
    `_to_delete/`. ONE builder per film, per repo convention.
  · Ledger: craft **145** (L144 whole-clip order, L145 declared-vs-measured light,
    L146 a rule in a RESUME line is not in the system - it must land in doctrine +
    a check + the plan shape the same session).
  · **EXPECTED SIDE EFFECT, by design:** every OLD plan (panborneo, wrx, crown,
    mahua) now FAILS check 38 - they are historical burst-cut films written before
    the rule was mechanical. Same class as planqc 23 blocking stale-acked plans.
    They stay blocked until re-cut whole-clip or given written CUT_JUSTIFICATION.
    Nothing was retro-edited: their delivered videos are untouched.
## HIS SECOND VERDICT ON V2 — THE PLANNING DEFECT (L147, end of session 11)
"When I hear of a vlog to Batu Niah, my first mindset is packing my stuff, road
trip, then hiking. This one just suddenly jumps inside. It does not show the
audience how do I get there. People like to see videos that have process in it."
CORRECT, and the root cause is precise: **the reference scan researched the
SUBJECT and never the FORM**, then file 31's drama rule ("never establish") was
applied to a VLOG pillar, where the getting-there IS the content.
Fixed in three existing files (no new files, his order):
  · `CLAUDE.md` step 2 — TWO scans now mandatory: SUBJECT **and** FORM.
  · `31-shortdrama-scriptwriter.md` — new SCOPE section: drama grammar rules the
    cinematic pillars; for vlog pillars the JOURNEY overrides "never establish"
    (open on an event INSIDE the departure, not at the destination).
  · `planqc.py` **check 39 JOURNEY** (blocking, vlog pillars only) — plan must
    declare JOURNEY {depart, transit, arrive}, in order, landing by 60%, or state
    JOURNEY_WAIVED with a reason. **plans/niah.py now correctly BLOCKS on it** -
    the gate reproduces his catch. Non-vlog pillars pass untouched (verified wrx).
  · Ledger: craft **146**, travel vlog **9** (tvL9 the journey is the product).

## PLANNING-PHASE KNOWLEDGE UPGRADE (his order, end of session 11)
"Do not fix anything first. Just look at how we can upgrade the planning phase.
Do not remove anything, just keep ADDING knowledge... that's why I put the
Japanese and Korean reference information there — it's for the plan phase to
LEARN." Purely additive, all inside `31-shortdrama-scriptwriter.md` (76 -> 227
lines):
  · **PART E — STORY SPINES.** Three-act (conflict, proactive character) vs
    **KISHŌTENKETSU** 起承転結 (ki/shō/ten/ketsu — discovery-driven, no antagonist
    needed, character RESPONDS, the TEN re-frames everything before it, biggest
    element held to the 3rd of 4 acts). **THE INSIGHT (L148): kishōtenketsu is
    this channel's native spine** — a cave, a road and a car have no antagonist,
    so three-act manufactures fake stakes. Includes the decision rule table and
    NIAH mapped act by act.
  · **PART F — THE TRIPLE LINK (L149).** Every boundary declares a PICTURE link,
    a SOUND link and a STORY link, all before spend. Only a picture link = a
    transition; all three = a story beat; a film of transitions is a showreel.
    Plus the free READ-ALOUD TEST: story links in order must read as a paragraph
    to someone who has not seen the footage, or the ORDER is wrong.
  · **PART G — 8 K/J craft principles** for the planner to learn from (contextual
    turns, hold the big element, characters respond, end aimed at the next,
    mundane setting + outsized feeling, one object carries the backstory,
    wordless emotion, restraint as technique). Kept as a growing list.
  · PART D checklist extended 6 -> 12 questions (spine named · journey beats ·
    read-aloud test · triple link · the recurring object · which card to delete).
  · Ledger: craft **148**.
  · **BOTH CANDIDATES BUILT AND REGRESSION-TESTED (his go, same session):**
    - `tools/storyboard.py` renders a **TRIPLE LINK panel per boundary** —
      PICTURE / SOUND / STORY, badged "STORY BEAT" at 3/3 or "TRANSITION ONLY —
      n/3", with undeclared channels shown as a RED GAP (the board never hides
      what was not decided). Renders only when the plan declares `LINKS`;
      plans without it render byte-identically to before.
    - `planqc.py` **check 40 TRIPLE LINK**. Graduated on purpose:
      `TRIPLE_LINK_BLOCKING = False` at module level -> a plan with no LINKS gets
      a WARN naming the exact boundaries that owe a sound link (every boundary
      touching a foreground FOLEY >= -6dB); a plan that HAS adopted LINKS is held
      to it and FAILS on a missing story or foreground-sound link. Flip the flag
      to make absence blocking - HIS CALL, one line.
    - `plans/niah.py` now carries a real 9-boundary LINKS block (the worked
      example + the read-aloud test material). planqc 40: **OK 9/9**.
    **REGRESSION EVIDENCE (his order to test before trusting):** old vs new
    planqc diffed across ALL 10 loadable plans -> **0 lines of behavioural change
    outside the new check**; storyboard re-rendered for niah/panborneo/wrx/crown
    with no crash and TRIPLE-LINK markup present only where declared (9 vs 0);
    14 pipeline modules compile; lessons_book regenerates; ledger loads.
    Backups: `_backup_20260812_links/{planqc.py,storyboard.py}`.
    Pre-existing, NOT caused by this work: `plans/i8.py` predates the plan format
    (missing TARGET_S/PROBE_FIRST) and `s450` has no .py plan at all.

## THE REFERENCES NOW DRIVE THE PIPELINE (L150-L152, his order: "hybrid what you
## learn from the reference INTO our pipeline… instead of creating a new part")
23 reference films were READ, not just measured — `refsense.json` had carried
numbers since 2026-08-06 with **every READ slot null**. Filled 11/11 for the two
scanned pillars; findings in file 31 PART H. Then each finding became BEHAVIOUR,
all inside existing files:
 1. **The spread, not the target.** `PILLAR-PROFILES.json` gained
    `measured_spread` per pillar (refs measured · cuts/min range · median range ·
    first_cut_median · open_grammar · text_register). **planqc 2** prints the
    reference range beside the target and PASSES a plan sitting inside the refs'
    own spread even when it misses ±20% of the mean. travel_vlog's six refs run
    22.2–59.6 cuts/min — the "40.3" was always a centre of mass.
 2. **Per-pillar hook ceiling.** **planqc 9** now takes its length ceiling from
    `first_cut_median_s`: car_cinematic gets **4.07s** because its own five refs
    hold that long before the first cut; pillars with nothing measured keep the
    2.0s doctrine. Measurement where measured, doctrine where not. The EVENT
    requirement never moved. (Verified live: supra reads 4.07s, crown 2.00s.)
 3. **Text register per pillar.** `capcards.py` gained REGISTERS +
    PILLAR_REGISTER: `quiet` (vlog whisper — tiny lowercase, barely-there scrim),
    `display` (cinematic — huge type, NO scrim, letters may crop), `card`
    (review — white card, dark text, subject+price), `punch` (industry, and the
    unchanged default). Each card carries its own `y`.
 4. **The journey mechanic, as a renderable thing.** The `time` register draws the
    timestamp chip at y=0.09 so a clock (7:00 → 8:00) can ride above normal
    captions — the process made legible for one caption per shot, ZERO extra
    generation. This is what NIAH V3 should use.
 REGRESSION (his order to test): default card render **hash-identical** to before
 (558x89 SABAH, unchanged), 9 plans run with no crash, **NIAH rebuilds at exactly
 1477 frames**, all modules compile, profiles load. Backups in
 `_backup_20260812_links/`. Ledger: craft **151**.
 STILL OWED: car_review + industry `--scan` (their long files timed out remotely —
 one command on his box); their READ findings are already in PART H.

## PHASE BOUNDARY CONFIRMED + TWO FIELD UPGRADES (L153-L154, session-11 close)
He asked whether the new work sits ONLY in the planning phase. MEASURED, not
claimed: `measured_spread` is a new key and engine/verify/clipqc/board have **0
references** to it; planqc runs at plan time; storyboard is a gate artefact.
**One exception he was right to catch:** `capcards.py` is a BUILD-phase file, so
its registers were fixed by moving the DECISION into the plan —
`CARD_REGISTER` now lives in `plans/niah.py` and the renderer merely obeys.
Rebuilt: 1477 frames, unchanged.
**HIS PIPELINE STATEMENT IS NOW CLAUDE.md DOCTRINE** (above THE CONTRACT):
research → plan EVERY single thing → storyboard → HIS APPROVAL → everything
downstream executes and never decides. *If a downstream stage has to make a
choice, that choice was missing from the plan, and the plan is what gets fixed.*
Web research kept only what we lacked (rejected as already-had: storyboard-as-
generation-brief, plan-as-data, reference-locked identity, shot-list specs):
  · **THE PROMISE** — every 2026 retention framework puts a clear promise at
    3-5s (hook 0-3 → promise 3-5 → body → payoff). CONTENT now takes
    `promise` / `promise_at` / `payoff_at`, validated by **planqc 18b**
    (graduated: absent = named warning, so no existing plan breaks). niah:
    promise@4.9s → payoff@34.5s.
  · **THE CLIP PACKAGE** — name the standalone moments BEFORE generating.
    `CLIP_PACKAGE` declares which shot sets stand alone as their own posts and
    **planqc 17 now prints credits PER POST**: niah 233cr / 3 posts = **77.7cr
    each** instead of 233. The cheapest post is the one already paid for — this
    attacks the zero-posts gap directly.
  REGRESSION: 9 plans run, no crash, planqc compiles, niah still 45 checks.

## QC REBUILT ON THE REFERENCES (L155-L157, his order, end of session 11)
His three complaints, each traced to a cause and fixed:
 1. **"the QC is not doing its job"** → `verify.py` line 55 fell back to
    `PROJECT="lc300"`, so any film verified without `--project` was scored against
    our FIRST build's plan. 11 of the 16 failures he saw were artefacts of that.
    FIXED: no fallback; project comes from env / `--project` / **inferred from the
    video path**; the pillar is RECOMPUTED after parsing (it was read at import,
    before `--project` existed); with nothing resolvable, check 5b fails loudly.
 2. **"the references are the baseline"** → new **check 20 REFERENCE BASELINE**,
    PICTURE ONLY per his scope (planning + rough cut, before Remotion): cuts/min,
    median shot, first cut, **palette carry**, all derived from
    `assets/refs/<pillar>/*.mp4` into `PILLAR-PROFILES.json.picture_baseline`.
    **Audio deliberately excluded** — the refs are platform re-encodes (2 of 6
    peak above 0 dBFS) and would bless clipping; his mix tuning stands.
 3. **"no linkage between scenes"** → now a NUMBER: palette carry.
    **NIAH 0.62 vs a reference floor of 0.66 = FAIL.** PANBORNEO_V5 (his ear
    approved) 0.83 = pass. The gate passes the film he approved and fails the one
    he criticised, untold which is which.
 Plus: `tools/build_niah.py` now writes `audio/niah_cuts.json` +
 `tmp/manifest_peaks.json` (schema READ from verify's checks 7/12, not guessed),
 so **12 checks that answered "NOT MEASURED" now actually run** — 11 OK.
 Backups in `_backup_20260812_links/`. Two calibration bugs found ONLY by testing:
 palette carry must be one-sided, and whole-clip must be read from the PLAN
 (the auto-detector found 20 segments in a 10-shot whole-clip film).

## REAL DEFECTS THE WORKING QC FOUND IN NIAH_V2 (ranked, none fixed yet)
 1. **True peak +1.2 dBTP** (spec ≤ −1.0). PROVEN cause, not guessed: the limiter
    is innocent (clamps a +6 dBFS test signal to −1.00); the rough cut is clean at
    −0.04; the finalmix chain outputs −0.83 as WAV and **−0.09 as AAC — the encoder
    adds +0.75 dB**. No codec headroom. TESTED FIX: limiter 0.794 (−2 dB) with
    TP −1 lands −1.01 dBFS. One flag change in the finalmix call.
 2. **Cuts are off the beat: median 60.5 ms, only 33% within 50 ms.** Root cause
    found: `--bed-ss 90.0` = **146.25 beats** at 97.5 BPM — the bed starts a
    quarter-beat late and every cut inherits it. Beat-aligned starts near there:
    **89.846s or 90.462s**. Free fix, one number.
 3. **Palette carry 0.62 < 0.66** — the linkage weakness, planning-phase fix
    (order, transition choice, grading continuity), not more cuts.
 4. **4 shot pairs read as the same image** (shot 4 vs 5/6/7 and 0) — the cave
    interiors look alike; a plan-level framing/lighting spread problem.
 5. Card collision flagged at 19.8–21.6s — UNVERIFIED, may be rock texture read as
    glyphs. Needs an eye before it is believed.

## HIS THREE PIPELINE ADJUSTMENTS — IMPLEMENTED IN THE FOLDER (L158)
Written where every future session will read them, not just answered in chat:
  · `CLAUDE.md` — new section "THREE ADJUSTMENTS HE MADE TO THE PIPELINE",
    placed ABOVE the planning doctrine so it is read before anything runs.
  · `START-NEW-CHAT.txt` — the same three, in the paste-into-a-new-chat file.
  · `docs/REMOTION-MOGRAPH-SPEC.md` — the automation order now carries the LOOP
    (edit QC gate -> back to ffmpeg on fail -> mastermind -> him).
  · `ledgers/knowledge.json` L158 — so every premortem reads it.
  1 TITLE = INTENT BRIEF (what he WANTS OUT of it, not just the subject).
  2 JUDGE EVERY CLIP after the batch, individually, before assembly.
  3 EDIT LOOP: ffmpeg->sound->polish->EDIT QC; fail loops back to ffmpeg;
    pass goes to MASTERMIND (file 27); its fail also loops back; only its pass
    reaches HIM, the final final boss. The loop RE-EDITS, never regenerates.

## NIAH V3 — COSTED, WAITING ON HIS GATE
The film needs 4 journey clips prepended; the re-cut is free and whole-clip.
Proposed (verify the river-crossing detail before writing the plan):
  DEPART   bag zipped + thrown in the boot, door slam, dawn — the EVENT hook
  TRANSIT  the Miri-Niah road from the driver's seat, jungle running past
  CROSS    the longboat over the Niah river (the real way in - VERIFY)
  ARRIVE   boots off the boat onto the park path, the massif ahead
Cost: 4 x 22.5 = **90cr** (+4cr if a jetty plate is wanted). Balance 3,364.61.
Runtime becomes ~69s whole-clip, or ~49s if the destination act is trimmed to
6 clips - HIS CALL at the gate.

- STILL ZERO POSTS. Two films sit finished: PANBORNEO_V5 (his ear approved) +
  NIAH_V2 (whole-clip, gated). The standing gap outranks everything above.

## ROOT CLEANUP (his ask, session 11)
Moved (mv, renames land on his PUSH): RESUME-b → archive/resumes/ · FINISH.bat +
BUILD_WRX.bat → archive/old-bats/ · PANBORNEO_V5.mp4 → projects/panborneo/
(untracked media, publish from there). Left deliberately: numbered crew docs
01-31 (cross-referenced by number everywhere — a crew/ migration needs a
dedicated reference-sweep session, HIS CALL), BGM/ (plans reference it),
ffmpeg-extracted/ (his local ffmpeg, BUILD scripts PATH), _backup_* (gitignored
local safety, RESUME paths point into them).

## SYNC (L132/L133/L135)
Truth channel = direct writes on clean bba204f. NEW: tools/capcheck.py,
tools/capcards.py, tools/build_panborneo_v6.py, this RESUME. EDITED:
ledgers/knowledge.json (3 lessons, backup taken). NO commits from the bridge
(read-only). **He runs PUSH.bat** (commits + pushes + self-verifies) and updates
START-NEW-CHAT.txt pointer to THIS file.
