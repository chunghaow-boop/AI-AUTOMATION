# RESUME — 2026-08-05 (remote session: v9 verified + first RED-TEAM day)
### A new Claude session reads CLAUDE.md, then THIS, and is fully caught up.
### Older context: RESUME-2026-08-04.md (the WRX build day), ledgers/.

## WHERE THE WRX STANDS — v9 IS THE SHIP CANDIDATE, WAITING ON HIS EYES ONLY
- Overnight build after the delogo commit wrote WRX_CINEMATIC_v1.mp4 (version
  counter bug, see finds) — VERIFIED to be the delogo build and RENAMED to
  WRX_CINEMATIC_v9.mp4. The original v1 was overwritten and is lost; v2-v9 intact.
- v9 measured on the delivered file: badge-zone red px 0 (v8: ~400; SR script
  GONE, recess clean, no patch artifact), 20.80s, -9.4 LUFS, -1.4 dBTP (mix
  re-rendered; still passes), sustained dips 0.
- JUDGES RE-WALK: J4's veto LIFTS. All other verdicts carry from v8 (J0 8/10,
  J3 75/100 FRESH, J5/J6 pass). v9 = SHIP pending: (1) his headphone + phone-
  speaker ear pass, (2) his eye on the RESIDUAL small garbled badge at the
  boot's RIGHT edge (~22px native, reads as "a badge" at speed - his call;
  killing it needs multi-box DELOGO, a 5-minute engine change, next build).
- THEN POST. Still 0 posts. The retention curve is the next real gate.

## RED-TEAM SESSION (his standing order: predict defects by BREAKING the
## pipeline in the sandbox, before reality does). 6 FINDS, ALL FIXED SAME DAY:
1. VERSION COUNTER (found before the session even started): engine default
   output was hardcoded _v1 -> overwrote approved history. NOW: scans output
   dir, takes next free version, never clobbers. (lesson 36)
2. DELOGO out-of-frame passed planqc -> planqc 25 "field sanity" blocks;
   engine also refuses loudly. (lesson 37)
3. SFX_OVERLAYS past clip EOF passed -> ffmpeg extracts SHORT silently, the
   covered dip ships uncovered. planqc 25 blocks (clip fit AND post-blend
   timeline fit). (lesson 37)
4. NON-ASCII CARDS ("你好 WRX 🔥", 3 words) passed word count -> fallback fonts
   render identical TOFU boxes (same .notdef bbox = the tell). planqc 25
   blocks unless CARDS_NON_ASCII_OK declared after a measured glyph test.
   (lesson 37)
5. STALE CACHE IDENTITY: segment+blend cache specs matched basenames only.
   A re-rendered segment served a STALE cached blend (old pixels in the seam);
   a re-downloaded clip would serve a stale segment. The delogo build dodged
   it only because shot 15 touches no blend. Both specs now carry size+mtime
   of their inputs. (lesson 38)
6. FOLEY AUDIBILITY: a spec-correct SILENT clip on a foreground foley shot
   passed clipqc (engine notes + builds anyway). clipqc now gates: source
   mixed >= -6dB must carry audio, mean > -45dB. Tested both directions.
   Corruption classes (truncated / 0-byte / black / wrong-spec) all already
   failed LOUD - held. (lesson 39)

## RED-TEAM WAVE 4 (same day, later): VERIFY.PY ATTACKED WITH v9. 2 MORE FINDS:
7. VACUOUS PASS - the class of the day: verify without its build context printed
   OK on 8/13 checks that measured NOTHING ('0/0 cuts (0%)' = PASS, 'no plan
   module' = PASS). A wrong/stale file could be blessed. FIXED: every cannot-
   measure branch now FAILS with 'NOT MEASURED - <why>'. Post-build on the
   desktop nothing changes; lying is now impossible. (lesson 40)
8. DEAD ARGUMENT: verify --project was parsed and NEVER used - paths were fixed
   at import to the lc300 default, so --project wrx verified against the wrong
   project's manifests. Recomputed in main(). Bonus: the long-open check-6
   'no CARDS found' blind spot is CLOSED by the same two fixes. (lesson 40)
TOTAL: 8 finds, 8 fixed, 0 credits, in one red-team day.
PUSH.bat is now SELF-HEALING (deletes stale git locks + bridge leftovers every
run) - remote sessions no longer bridge-commit; PUSH.bat owns git entirely.

## STATE OF THE GATES
- planqc: 27 checks (22 premortem / 23 lessons_ack / 24 linkage / 25 field
  sanity all new this cycle). wrx passes 27/27 with LESSONS_ACK = 39.
- Ledger: 39 lessons. clipqc: +foley-audibility, +text-zoom evidence.
- redteam/mkplan.py committed - regenerates the 8 hostile plans; the rt_*
  plan files themselves are generated, not committed.
- Cloud container facts (confirmed): playwright+chromium WORK, tesseract
  present, cv2 pin 4.10.0.84 installs clean.

## NEXT (in order)
1. HIS EAR + EYE on v9 -> POST. Nothing else outranks this.
2. NEXT BUILD picks up automatically: version-monotonic naming, cache identity
   specs, DELOGO guard. Full segment re-render expected (spec format changed).
3. verify.py red-team DEFERRED (needs full build artifacts to attack honestly).
4. ingest.py luma/motion means -> linkage 24b goes live.
5. engine crf/bitrate chain STILL TODO. Multi-box DELOGO if his eye wants the
   right-edge badge gone. Frame-zero clipqc window-head check (yesterday's #2).
6. Balance: unchanged, ~5,203 est. MEASURE before any spend (hard rule 2).
