# RESUME — 2026-08-12b (session 10, Cowork) — FINAL, session closed cleanly

Read order: CLAUDE.md → SYSTEM-MAP.md → this. (HANDOVER-PASTE.md is history now — repo is current.)

## THE ONE-LINE ANSWER HE EXPECTS
**PANBORNEO_V5.mp4 is built, gated (bedcheck PASS +3.5dB bed-over-foley), and his
ear APPROVED the audio (APV-4). It sits in repo root, UNPOSTED. Sync verified at
076f4d7 both sides (newer if he pushed since). NEXT: he runs REORG.bat+PUSH.bat,
PUBLISHES V5 (AI label + compression caption + LiQWYD credit), then ACE Studio MCP.**

## END-OF-SESSION STATE (written at close, 2026-08-12 ~15:20)
- **V5 AUDIO APPROVED BY HIS EAR** — "the audio is well done" → `ledgers/approvals.json`
  **APV-4**, scoped to the V5 mix only (not cards, not duration). bedcheck v2's PASS
  predicted his verdict — the gate measures what he hears. L128 flag values
  (--foley-target -22 --bed-under -4 --duck-ratio 2 --duck-release 300) are THE
  travel_vlog finalmix standard now.
- **Ledger counts at close (MEASURED): general craft 135 lessons · travel vlog 8 · approvals 4.**
  (planqc 23 will block stale-acked plans until re-ack — correct behavior.)
- **PUSH.bat v2 got a message fix** (network-vs-conflict wording) — uncommitted at
  write time, rides his next push.
- **REORG.bat v2 refreshed, NOT YET RUN** — archives 11 old RESUMEs + 7 superseded
  root docs + docs/ leftovers via git mv (nothing deleted). He was about to run it.
  If root still shows RESUME-2026-08-07.md etc., it hasn't run yet — remind him.
- **ACE STUDIO MCP — his chosen AI-audio direction, setup pending on his box:**
  ACE Studio app → Preferences → General → MCP Server → STDIO → Copy Command →
  paste into %APPDATA%\Claude\claude_desktop_config.json as mcpServers.acestudio
  (command + ["--stdio"]), restart Claude Desktop, tools appear in the NEW session.
  It is a music/vocal studio (BGM composer seat — rights-owned beds at exact pillar
  BPM), NOT an SFX generator. Experimental per its own docs. First job: compose a
  97.5 BPM test bed vs travel_vlog profile, A/B against LiQWYD BY MEASUREMENT.
  ElevenLabs connector = voice agents only; parked until talking pillars exist.
  Higgsfield generate_audio has an elevenlabs TTS variant for Nev's voice.

## WHERE WE ARE
- **main = 00cc146 VERIFIED, local AND GitHub** — the 27-commit bundle landed on
  FINISH run #7 (FINISH3.bat, reset --hard + self-verify). Six prior silent
  failures diagnosed and lessonized: `LESSONS-SESSION10-PENDING.md` (L128–L134,
  MERGE INTO knowledge.json NEXT SESSION, then delete that file).
- **Balance measured 3,977.57cr (team)** at session start. Zero spent this session.
- **panborneo v4 (64s) analyzed**: his verdict = best build yet. Measured: story
  turns + locality cards beat cutting-DNA conformance. BUT audio fails house
  gates (-10.6 LUFS / -0.1 dBTP as chat-delivered) and melody band sits -40dB
  flat → the bed is buried under foley (L128). HE CONFIRMED: travel_vlog is
  BGM-led, foley UNDER bed.

## DONE THIS SESSION (all additive, no pipeline-flow edits)
1. `FINISH3.bat` — the sync pattern that finally works: reset --hard FETCH_HEAD
   + self-verifies HEAD and ls-remote before printing DONE.
2. `tools/finalmix.py` — constants → CLI flags (--foley-target --bed-under
   --duck-ratio --duck-release), defaults byte-identical (proven), policy echoed
   in evidence JSON. Backup: `_backup_20260812_prefinish/finalmix_pre-L128.py`.
3. `31-shortdrama-scriptwriter.md` — NEW SEAT DOC: K/J short-drama + anime-short
   grammar (7 rules) mapped onto the CONTENT block. planqc candidates listed, HIS CALL.
4. `docs/SHORTDRAMA-REFS.md` — 6 genres × ≥2 refs, fetchability flagged, for
   reffetch on his box then refsense READ teardowns.
5. `docs/REMOTION-MOGRAPH-SPEC.md` — Remotion(free ≤3ppl) takes graphics/cards
   motion layer; ffmpeg keeps cuts+audio+encode. PoC on his box, composite as a
   finalmix-style post-step, re-gate after. Zero engine edits.

## NEXT ACTIONS, IN ORDER (1/3/4 are HIS BOX — clips absent from mount, YouTube 403 from container)
1. HIS BOX: remix v4 —
   `python tools\build_panborneo_v4.py` then
   `python tools\finalmix.py --video panborneo_V4.mp4 --bed assets\bank\bgm\travel_vlog\liqwyd-to-the-moon.mp3 --bed-ss 9.57 --whoosh assets\bank\sfx\whoosh_whip.wav --whoosh-at 19.37 --whoosh-gain 4 --foley-target -22 --bed-under -4 --duck-ratio 2 --duck-release 300 --out PANBORNEO_V5.mp4`
   then `python tools\bedcheck.py PANBORNEO_V5.mp4` — must PASS (v4-chat FAILS at 29.5dB gap, validated).
2. ~~Merge lessons~~ **DONE** — craft 133 / travel vlog 8. All stale-acked plans now planqc-23-BLOCKED (by design).
3. HIS BOX: reffetch the [YT] rows of docs/SHORTDRAMA-REFS.md → refsense --fill (READ, not just measure).
4. HIS BOX: Remotion PoC per docs/REMOTION-MOGRAPH-SPEC.md.
5. PUBLISH panborneo V5 (J4 conditions in HANDOVER-PASTE: compression caption, AI
   label, LiQWYD credit). STILL ZERO POSTS — the standing gap outranks all of this.

## ADDED AFTER THE FIRST WRITE OF THIS RESUME
- `tools/bedcheck.py` — the L128 mechanical gate (melody band within 12dB of
  broadband, exit 0/1). VALIDATED: fails v4-chat at 29.5dB gap and reproduces the
  +10.7dB low-band lift to the decimal.
- knowledge.json merged (backup `_backup_lessonize/knowledge_pre-session10.json`);
  `LESSONS-SESSION10-PENDING.md` is now a stub, park in `_to_delete\`.
- Container access measured: YouTube 403 (proxy), same class as CDN/chosic —
  reference fetching is a HIS-BOX job, permanently.

## SYNC CONSOLIDATION (his catch, late session 10) — L135, craft now 134
He spotted it: "multiple ways of pushing is causing bugs." Measured and fixed:
- **FINISH3.bat was an ARMED landmine**: repo at 277f655, script still reset
  --hard to the OLD bundle head 00cc146 — one double-click = rollback + wipe,
  with a self-verify that would have printed VERIFIED. Parked in `_to_delete\`
  with FINISH2.bat, PUSH-NOW.bat, talyx-FINISH.bundle (PUSH.bat's janitor
  deletes that folder on next run).
- **FINISH.bat** (tracked) → loud RETIRED stub.
- **PUSH.bat v2**: v1 hid pull errors (`2>nul`) then `git add -A` — a conflicted
  pull would have committed conflict markers silently. v2: commit FIRST → pull
  --rebase errors visible, STOP on conflict → push → ls-remote self-verify.
  v1 backed up in `_backup_20260812_prefinish/PUSH_v1.bat`.
- END STATE: exactly TWO sync scripts. PULL.bat sit down, PUSH.bat stand up.
- Optional cleanup, HIS CALL: 6 stale `pre-finish-backup` stashes —
  review `git stash list`, then `git stash clear`.

## V5 — BUILT REMOTELY, GATED, DELIVERED (end of session 10)
Clips found in his mounted Downloads → V4 rebuilt IN THE SANDBOX (frame grid
1536/64.0s/whip 19.708 = spec exact) → finalmix L128 numbers → peak fix.
- **`PANBORNEO_V5.mp4` in repo root. bedcheck v2: PASS, bed +3.5dB over foley.**
  Old v4 measured the SAME way: −31.3dB (his ear, now a number). I −11.0 LUFS,
  TP −1.0 (v5a overshot to +0.5 dBTP; audio-only remaster with alimiter 0.794).
- **DECLARED DEVIATION: cards render in CapCutSansText-Bold** (repo font, the
  engine's own first candidate) — container had no Montserrat and apt is
  root-blocked. His eye decides; Montserrat re-render on his box if wanted.
- finalmix.py gained aformat channel-layout pins (strict-ffmpeg fix, no-op on
  his box). bedcheck v2 = projection method (L136: v1's spectral threshold was
  vacuous — the bed solo measures a 28dB melody gap; calibrate gates against
  the reference signal, align by cross-correlation before projecting).
- Craft lessons now 135.
- AT PUBLISH (unchanged, J4-binding): compression caption · AI label · LiQWYD credit.

## SYNC DISCIPLINE THIS SESSION (L132)
Truth channel = DIRECT WRITES on top of clean 00cc146. New untracked files listed
above + one tracked edit (tools/finalmix.py). NO bundle this session. He commits/
pushes on Windows (PUSH.bat); bridge git stays read-only --no-optional-locks (L133).
