# RESUME — 2026-08-06c (session 5 continued, remote/Cowork)

Read order: `CLAUDE.md` → `SYSTEM-MAP.md` → this file. Nothing here is optional.

> **`SYSTEM-MAP.md` gained section 11 — THE LEARNING LOOP.** Read it. It is how a
> finding becomes permanent, and it is the answer to "does the mastermind know this?"
> The mastermind is NOT a program. It is a seat executed by a human plus an LLM, so
> **its entire memory is these files.** Anything not written down does not exist.

---

## WHERE WE ARE

**Balance: MEASURED 4,927.82 cr, plan `team`, 2026-08-06. ZERO credits spent all session.**

**`smoketest.py` is 79 passed / 0 failed.** It has never been clean before. The old
74/5 baseline is retired — four of those five were one bad glob, the fifth was an
unescaped Windows path.

`plans/kundasang.py` — planqc **PASS 34/34**, judges **SHIP YES** (J0 8 · J2 7 · J4 8),
**20 premortem risks**, acked at craft 76. **233.0cr total / 30.5cr probe.**
Nothing generated. **Plates `glc300` + `kundasang` NOT BUILT (8cr).**

**NEXT ACTION:** build the two plates (8cr) → LOOK at them → probe source A alone
(22.5cr) → judge the tear timing → then the other nine clips (202.5cr).

---

## THREE PIPELINE BUGS FIXED, ALL MEASURED FIRST

| file | bug | proof |
|---|---|---|
| `engine.py` | **card font was LINUX-ONLY on a Windows pipeline.** Two `/usr/share/fonts` candidates, neither exists on Windows → FONT pointed at a missing file → drawtext rendered **NOTHING**, silently. ffmpeg does not reliably error on a missing fontfile and **no gate reads pixels for text**, so a card could vanish from a DELIVERED video with planqc still green. | 5 candidates, repo's own `CapCutSansText-Bold.otf` FIRST, prints the chosen font, prints a loud list if none, and RAISES rather than shipping empty cards |
| `tools/smoketest.py` | **4 failures from ONE bad glob.** `assets/bgm/utility-beds/*.wav` is a symlink to `BGM/`, which holds `.mp3` per pillar and no `.wav` at that level → matched nothing → no bed → tail/hook at −180 dB → loudness had no energy to converge on. The 8 beds were one directory up the whole time. | search widens cheapest-first: 0 → 8 wav / 46 mp3. audio 3→6, loudness 1→3 |
| `tools/smoketest.py` | **it is the BACKSLASHES, not just the colon.** After escaping the drive colon in the font path the render still failed: `[AVFilterGraph] No option name near '\Users\User\AppData\Loca...'`. The filtergraph parser treats `\` as an escape AND `:` as a separator, so the `textfile=` path on the SAME filter broke it. | one `_ffpath()` applied to every path entering a filter. `caption cards render` FAIL → PASS |

`engine.py` was re-checked against that last class: it passes only `text=` (a literal,
already escaped) and `fontfile=` (now escaped), never a path to `movie=`/`textfile=`.

---

## THE MIX — HIS CATCH, MEASURED BEFORE ANYTHING CHANGED

> *"the sfx and foley are too loud it covers the whole bgm"*

Flat RMS proved nothing (foreground shots only **+0.6 dB** over background). That is the
tell: a sidechained bed keeps TOTAL level constant while the MUSIC disappears. The
signature is spectral.

**Bed's 40–160 Hz band during the loudest 10% of broadband moments:**

| build | drop |
|---|---|
| KK v15 — travel_vlog, `hero_only` | **−4.9 dB** — ordinary musical ducking |
| **WRX v9 — car_cinematic, `edit_sfx=full`** | **−31.1 dB** |

−31 dB is the music being REMOVED under every whoosh. Cause: foley foreground hardcoded
at bed−2, edit-sfx at bed−6, sidechain 0.06/6:1 — **for every pillar alike.**

`engine.py` now reads 5 mix knobs from the PILLAR style block, **defaulting to the old
constants** (verified byte-identical: the sidechain string is unchanged and
`bl + (−6.0) == bl − 6.0`). travel_vlog declares **foley foreground bed−8**, duck **3:1
at 0.10 / 180 ms**.

**car_cinematic is UNCHANGED and OPEN — HIS CALL.** Retuning it changes the approved
WRX look. The number to decide with is −31.1 dB.

---

## THE WHIP — HE WAS RIGHT, THE DETECTOR WAS BLIND

> *"i created a whole lot of transition banks inside it"*

Correct. `tools/fx.py` holds **14** transitions and `engine.py:596` dispatches them live.
travel_vlog declared 0% blended from a frame-difference heuristic whose **own analysis
file, line 86**, says: *"catches wipes and fades reliably, but a very fast whip can read
as a hard cut."* A fast whip is exactly what a vlog uses.

`tools/blendsense.py` (NEW) separates a **WHIP** (sharpness trench of 2–6 frames WITH a
scene change across it) from a hard cut (no trench) and a dissolve (sustained mid
correlation). Same 6 references:

| | cuts | hard | **whip** | diss |
|---|---|---|---|---|
| pooled across 6 refs | 42 | 38 | **3** | 1 |

**9.5% designed pooled. The old heuristic scored those same whips 0.0%.** 3 of 6 refs use
at least one. Per-file medians are useless here — two refs have only 1 and 2 cuts.

- profile now declares `designed_pct 9.5` · `designed_kinds ["whip"]` · `blend_max_count 2`
- planqc 11 gained a **COUNT band** (a percentage quantises to zero: 1 blend in 19
  boundaries is 5.3%, over a [0,5] cap) and a **kind whitelist** (a dissolve now FAILS)
- kundasang takes **ONE whip at boundary 9** — the cloud closes, SO he pulls over —
  because 3 of 6 refs use none. Conservative read of the same number.

---

## `tools/lessonize.py` — HIS ASK, BUILT AND PROVEN

> *"every time each QC found a bug or an error it automatically fix and passed it to
> mastermind so next time it wont make this mistake again"*

| half | status |
|---|---|
| **CAPTURE** | automatic. Failing gate check / judge seat → dated lesson in `knowledge.json`, deduped on a signature. |
| **BLOCK** | automatic. The count rises → **planqc 23 BLOCKS** every plan acked lower. |
| **`--brief`** | **PRINTS the unread lessons** for a plan. Added because planqc 23 checks a NUMBER, not comprehension — a session can ack 76 without reading one line. |
| **FIX** | **REFUSED, permanently.** A loop that edited a plan to clear its own gate optimises the CHECK instead of the FILM. |

**It caught its own first bug:** v1 read only the LATEST verdict, so kundasang's J4 veto
(run 1, cleared in run 2) filed **nothing** — the exact failure the tool exists to
prevent, in the tool itself.

**Craft lessons 64 → 76 today.** Every one of the twelve came from a real failure this
session. Each filing re-BLOCKED kundasang until it read them and re-acked — that is the
loop closing, not a nuisance.

```
python tools\lessonize.py <plan> --brief        the unread lessons, printed
python tools\lessonize.py <p> --from-judge      file every FAILED seat, all runs
python tools\lessonize.py <p> --from-gate       run planqc, file every FAIL
python tools\lessonize.py --status              counts + which plans are BLOCKED
```

---

## STILL OPEN — HIS CALL

- **`board.py` caps at 9 SOURCES.** Line 216 hardcodes a 3×3 grid; kundasang has 10 →
  `IndexError`. `storyboard.py` renders it fine, so nothing is blocked. Fix is
  `rows = ceil(n/3); cx = col_x[n // rows]`. **PIPELINE FILE.**
- **car_cinematic mix** — −31.1 dB measured, untouched pending his decision.
- **P2-1 motion floor** — no shipped KK shot came within 2.3× of the 0.6 floor. Decide
  from kundasang's raws at ingest.
- **23 refs measured, ZERO READ.** `assets/refs/travel_vlog/` has 6 files and no
  `refsense.json`. Highest-value zero-credit work left.
- `plans/wrx.py` missing `hero_shot` (LATENT — wrx runs `edit_sfx=full`).
- **crown, kk, wrx are planqc-BLOCKED on stale acks** (64/56/56 vs 76). Expected.

## KNOWLEDGE GAP THAT WAS FOUND AND CLOSED
An audit asked what a fresh mastermind actually reaches. **Five of six things built
today were invisible** to a new chat — `blendsense`, the mix knobs, `blend_max_count`,
the font fix and the new baseline appeared in **zero** of CLAUDE.md / SYSTEM-MAP.md /
the RESUME. All are now in SYSTEM-MAP §11. **Correction:** the previous RESUME says
`GATE.md` and `PROMPTS.md` do not exist — **they do**, at `docs/GATE.md` and
`docs/PROMPTS.md`. Wrong path, not a missing file.

## WHAT THE MASTERMIND ACTUALLY KNOWS — MEASURED, NOT ASSUMED

He asked twice: *"does my mastermind know this whole pipeline in detail?"* Answered by
diffing what the three entry files NAME against what is on disk:

| | named | on disk |
|---|---|---|
| root pipeline `.py` | **6** | 6 |
| `tools/*.py` | 32 | 53 |
| gate checks by number | ~41 | 68 |
| **numbered doctrine docs** | **1** | **28** |

**The spine is well covered. The DOCTRINE was invisible.** Every seat definition —
04 foley master, 06 content judges, 07 emotion engine, 08 the strategist, 10 the editor,
19 sound engineer, 25 qc debate, 27 mastermind qc — existed, and no new session was ever
told they were there. **SYSTEM-MAP section 12 now indexes all 28** and names the three a
mastermind must read before acting: **27, 06, 08**.

**STILL OPEN, HIS CALL: both skills predate the gates.**
`skills/talyx-cinematic/SKILL.md` (1,687 words) and `skills/talyx-shotlist/SKILL.md`
(645 words) mention **planqc 0 times, clipqc 0, judge 0, lessonize 0**. CLAUDE.md still
says "Use the `/talyx-shotlist` skill for Phase 1" — so obeying CLAUDE.md gets you
Phase-1 guidance written before the 68-check architecture existed. Rewrite them against
the current gates, or delete the pointer. **Do not leave both.**

**CORRECTION made this session:** an earlier claim here that `build_kk.py` does not exist
was WRONG — it is at `tools/build_kk.py` and `smoketest.py` imports it for the loudness
route. The claim came from an `ls` at repo root instead of `tools/`. Live infrastructure,
not legacy.

## STANDING RULES (unchanged)
- Never invent a number, path, credit cost or capability.
- **Claude cannot hear audio.** Every audio claim comes from a measurement.
- `device_bash` cannot delete — `cp`, then delete it yourself.
- **The Cowork bridge kills any command over 45 seconds.** `smoketest.py` takes ~90s, so
  it must be run BY HIM on Windows. Needs ffmpeg on PATH:
  `set PATH=%PATH%;C:\Users\User\Desktop\AI\ffmpeg-extracted\ffmpeg-9.0-essentials_build\bin`
- **ONE CHAT OWNS A PIPELINE FILE AT A TIME.** Two Cowork chats had write access to this
  repo today. Backups: `_backup_20260806b/` incl. `engine_WITH_MIXKNOBS.py`.
- Chat is disposable, **the repo is the memory.**

## THE STANDING GAP
Still **zero posts, ever.** The pipeline is now clean at 79/0, the plan ships at 34/34,
and the loop learns by itself. Not one of those can tell you whether a video works.
**One posted video with a real 24-hour curve outranks everything in this file.**
