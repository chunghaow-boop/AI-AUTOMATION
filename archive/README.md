# ARCHIVE — nothing here is deleted, and nothing here is current

Moved 2026-08-07 with `git mv`, after he said:

> *"there is alot of duplicated lessons, skills, knowledge, etc all in different
> different files but they share the same purpose, so maybe we can organize it?"*

Measured before moving (`DOC-AUDIT.md`): **71 markdown files, 46 at the repo root,
595 KB, and NINE of them all trying to be the entry point.** A new session read
whichever one it happened to open — the same failure as the dead `GATE.md` pointer that
made every session start cold.

**Every file here is intact and every line is in git history.** If something in the
current four turns out to be missing a detail, it is in one of these — go and get it.

| folder | what is in it | superseded by |
|---|---|---|
| `resumes/` | the 8 previous `RESUME-*.md`, 08-04 through 08-06c | the newest `RESUME-*.md` at the root |
| `entry-superseded/` | `RUNNER.md`, `SYNC.md`, `FOLDER-MAP.md`, `RECONCILE.md`, `README-old.md` | `README.md` + `CLAUDE.md` + `SYSTEM-MAP.md` |
| `docs-superseded/` | the 2026-07-31 and RESTORE-era handovers, `PIPELINE.md`, `PIPELINE-V2.md`, `V2-REBUILD.md`, `RUN.md`, `docs/RUNNER.md` | `SYSTEM-MAP.md` |
| `old-bats/`, `HANDOVER*.md`, `RESUME-2026-08-01.md` | pre-existing archive, untouched | — |

**What did NOT move:** every numbered doctrine doc `00-` to `28-`. Those are the SEATS,
they are indexed in `SYSTEM-MAP.md` section 12, and `ledgers/routing.json` points at
`22-HANDOVER.md` by name. Two of them carry a banner at the top saying they are no
longer an entry point.
