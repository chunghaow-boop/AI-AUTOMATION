# RESUME — 2026-08-11 (session 9, remote/Cowork)

Read order: `CLAUDE.md` → `SYSTEM-MAP.md` → this file.

## WHERE WE ARE, ONE LINE

Both gated plans (`desafarm`, `mahua`) pass planqc **38/38**; the pipeline fixes he
authorised ("help me fix it") are built and re-verified; FINISH.bat + a refreshed
bundle are on his desktop; **next input is a video file — the video test.**

## WHAT CHANGED THIS SESSION (all re-checked after fixing, hard rule 4)

| what | where | evidence |
|---|---|---|
| ONE MECHANISM for overlap blends (closed PENDING 2.3) | `engine.py` exports `BLEND_RESERVES_OVERLAP = True` beside the reservation code; planqc 34 imports it. A plan flag is now only an explicit opt-out. | planqc 34 OK on both plans with no plan flag declared |
| `SHOT_WINDOW = {shot: t_in}` per-shot pins (closed mahua open risk 1) | allocator in `engine.py`: pins allocate FIRST, a pin that does not fit is a loud allocation failure | mahua pins {16: 0.20, 18: 3.70}; desafarm {12: 0.20, 16: 3.60} — an arc can no longer arrive in reverse |
| verify check 5 REPORT-ONLY for travel_vlog | `style.exposure_gate = "report"` in PILLAR-PROFILES; `verify.py` honours it, measurement still prints in full | a six-light-state day no longer fails a blocking gate; car_cinematic unchanged (blocks) |
| check 33 derivation for travel_vlog | `ledgers/thresholds.json`: shot_match_clamp carries travel_vlog WITH the derivation — inert under `shot_match_max_move = 0.0` (his 08-07 instruction) | 33 OK, note states the clamp cannot bind |
| desafarm card collision (craft L107) | altitude card 14,4 → **14,2**; spans disjoint | planqc 12 OK |
| Both plans re-acked + RELATIONSHIPS | craft 101/96 → **115**, travel vlog 5 → **6**, plus the three neighbouring pillars (23b blocks now — i8 16 · car cinematic 15 · lc300 8, read; transferable: hooks-under-2s, single-event, fx.whip seam history) | 23, 23b, 32 OK; premortems extended with tv L5 + L102/L105 entries |

## WHAT DID NOT CHANGE

- **No credits spent.** All of the above is free work on plans, gates and engine.
- PENDING 2.1 (reference-video bank) is still the biggest item — **and the video test
  is its first sample.** 2.4 (two stale skills) still open.
- Zero posts, still. The standing gap stands.

## THE VIDEO TEST — how to run it from here

Cloud session has ffmpeg/ffprobe + the repo tools. He sends a video (attach in chat,
or drop into `Desktop\AI` / `Downloads` and say so). Then: measure — cut times, shot
lengths, motion curve, luma arc, hook timing, caption timing — and file the numbers
against the guesses in `ledgers/thresholds.json` (§3 of PENDING). If the file is one
of OUR cuts, run it against the plan's pillar profile instead (verify-style, remote).

## HANDOVER

His one action is unchanged: **double-click `FINISH.bat`** — it now applies ALL
session commits (08-07 + 08-08 + these) from the refreshed `talyx-FINISH.bundle`,
then pushes. The Cowork bridge was UP this session; bundle + bat were written
directly to `Desktop\AI`.

---

## SESSION 9 CONTINUATION — PHASE 2 RAN, AND THE PLATFORM STOPPED IT

**panborneo APPROVED and STARTED.** Balance re-measured 4,294.82 before spend.

**WHAT EXISTS (16cr MEASURED, 4,294.82 -> 4,278.82):** all 4 plates at 5504x3072,
RapidOCR CLEAN on every one including the Defender body. Job IDs in the plan.
Five nev face refs uploaded to Higgsfield (media ids in PLATES.nev). Wardrobe refs
are prompt-text only — upload.higgsfield.ai is 403 from the container (re-measured)
and the wardrobe files are not in the public repo; stated choice.

**WHAT IS BLOCKED: every video generation, by the PLATFORM.** 7/7 seedance jobs
returned status=nsfw, ALL REFUNDED. The isolation matrix cleared every variable we
control: verbatim G · softened G · park-gate reframe · different shot (I) · NO
reference + bland control · seedance_2_5 · generate_audio OFF. His own manual
web-UI generation (unrelated prompt) flagged the same minute. Account-wide filter
misfire = craft L117. Hard rule 5 held twice; both stops were asked and answered.

**RESUME PATH, one command deep:** face-ref media ids and plate job ids are in the
plan. Retry = one G probe (22.5cr, refunds if flagged); if clean, batch the other
12 and the film completes exactly as gated (308.5cr total). Nothing needs
re-planning; planqc still 39/39 at ack 117.

---

## 2026-08-12 01:33 — THE SCHEDULED RETRY FIRED, AND THE FILM'S FOOTAGE EXISTS

The Seedance filter had recovered (his 2.5-launch/promo theory: the clampdown
lifted overnight). The armed probe of G COMPLETED, passed remote QC, and the
remaining 12 batched immediately: **13/13, ZERO failures.**

MEASURED: 4,270.07 -> 3,977.57 = **292.50cr, exactly 13 x 22.5.** Total build
spend 308.5cr - the gated figure TO THE CREDIT, second build running the cost
model has been exact against real transactions.

REMOTE QC, all 13: 720x1280 · 5.062s · AAC audio present · OCR clean (no
legible text anywhere, including the border, boulevard and city shots) · luma
median 104, ZERO outliers (cabin C = 89.8 - the desafarm silhouette did not
recur) · the dawn->dusk light arc is visible in the per-clip means.

HIS BOX, in order (the sandbox cannot reach the CDN):
    python tools\pull_panborneo.py       13 clips -> projects/panborneo/clips/
    python talyx.py ingest panborneo     clipqc 13 checks/clip + contact sheet
                                         (READ it: arm visible in G's 3.30s
                                         window · H pour-to-shake · E leap order)
    pick the BED from BGM/travel_vlog    native tempo, zero stretch; recompute
                                         the 48-beat grid if not ~99.4
    python tools\ingest.py panborneo     manifest
    python talyx.py build panborneo      the cut
    python talyx.py verify panborneo     freshness first
Then the judges run ON THE CUT, and the compression caption (Brunei transit,
1,000+ km, multi-day) is the binding human step at publish - J4's condition.

AND STILL: FINISH.bat has not completed a push (the self-stash bug ate the
20:17 run; the fixed script is on the desktop). 18+ commits wait in the bundle.
