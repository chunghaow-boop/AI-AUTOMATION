# RESUME — 2026-08-11 (session 9, remote/Cowork)

Read order: `CLAUDE.md` → `SYSTEM-MAP.md` → this file.

## WHERE WE ARE, ONE LINE

Both gated plans (`desafarm`, `mahua`) pass planqc **38/38**; the pipeline fixes he
authorised ("help me fix it") are built and re-verified; FINISH.bat + a refreshed
bundle are on his desktop; **next input is a video file — the video test.**

## WHAT CHANGED THIS SESSION (all re-checked after fixing, hard rule 4)

| what | where | evidence |
|---|---|---|
| ONE MECHANISM for overlap blends (closed PENDING 2.3) | `engine.py` exports `BLEND_RESERVES_OVERLAP = True` beside the reservation code; planqc 34 imports it. A plan flag is now only an explicit opt-out. | planqc 34 OK on both plans with no plan flag declared |
| `SHOT_WINDOW = {shot: t_in}` per-shot pins (closed mahua open risk 1) | allocator in `engine.py`: pins allocate FIRST, a pin that does not fit is a loud allocation failure | mahua pins {16: 0.20, 18: 3.70}; desafarm {12: 0.20, 16: 3.60} — an arc can no longer arrive in reverse |
| verify check 5 REPORT-ONLY for travel_vlog | `style.exposure_gate = "report"` in PILLAR-PROFILES; `verify.py` honours it, measurement still prints in full | a six-light-state day no longer fails a blocking gate; car_cinematic unchanged (blocks) |
| check 33 derivation for travel_vlog | `ledgers/thresholds.json`: shot_match_clamp carries travel_vlog WITH the derivation — inert under `shot_match_max_move = 0.0` (his 08-07 instruction) | 33 OK, note states the clamp cannot bind |
| desafarm card collision (craft L107) | altitude card 14,4 → **14,2**; spans disjoint | planqc 12 OK |
| Both plans re-acked + RELATIONSHIPS | craft 101/96 → **115**, travel vlog 5 → **6**, plus the three neighbouring pillars (23b blocks now — i8 16 · car cinematic 15 · lc300 8, read; transferable: hooks-under-2s, single-event, fx.whip seam history) | 23, 23b, 32 OK; premortems extended with tv L5 + L102/L105 entries |

## WHAT DID NOT CHANGE

- **No credits spent.** All of the above is free work on plans, gates and engine.
- PENDING 2.1 (reference-video bank) is still the biggest item — **and the video test
  is its first sample.** 2.4 (two stale skills) still open.
- Zero posts, still. The standing gap stands.

## THE VIDEO TEST — how to run it from here

Cloud session has ffmpeg/ffprobe + the repo tools. He sends a video (attach in chat,
or drop into `Desktop\AI` / `Downloads` and say so). Then: measure — cut times, shot
lengths, motion curve, luma arc, hook timing, caption timing — and file the numbers
against the guesses in `ledgers/thresholds.json` (§3 of PENDING). If the file is one
of OUR cuts, run it against the plan's pillar profile instead (verify-style, remote).

## HANDOVER

His one action is unchanged: **double-click `FINISH.bat`** — it now applies ALL
session commits (08-07 + 08-08 + these) from the refreshed `talyx-FINISH.bundle`,
then pushes. The Cowork bridge was UP this session; bundle + bat were written
directly to `Desktop\AI`.
