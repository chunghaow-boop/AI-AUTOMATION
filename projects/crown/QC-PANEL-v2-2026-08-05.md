# CROWN v2 — PANEL RE-WALK, 2026-08-05

Both panels re-walked against `plans/crown.py` v2. **Judge: REDESIGN (scoped). Sound: FAIL.**
Still zero credits spent.

Real movement: J0 4→5, J3 68→70, ARC FAIL→CONDITIONAL PASS, and the sound panel's own words —
*"Real, large progress over v1. It still fails, on arithmetic and on inert declarations, not
on taste."* The redesign is now small and confined to **shots 1–8 and the card map**.
Everything downstream of shot 8 is sound.

---

## THE THING I HAVE TO OWN FIRST

**I invented two flags that no tool reads.** `BLEND_AS_OVERLAP = True` and `GENERATE_AUDIO
= True` are both declared in v2 as if they change behaviour. Both panels checked:
`grep BLEND_AS_OVERLAP planqc.py` = 0 hits, `grep GENERATE_AUDIO planqc.py` = 0 hits, and
`planqc.py:868` still reads `fit = total - len(BLEND_AFTER) * BLEND_WIDTH  # blends COMPRESS
the timeline`.

So the deliverable is still 28.80s, the hero still drifts off the 0.600s grid, and nothing
guarantees the clips come back with audio. **A declaration that no tool reads is a comment
wearing a switch's clothes.** This is hard rule 1 — never claim a capability — broken by me,
in the plan written to fix a defect list. It is also the P0-4 fix marked LANDED in my own
docstring, which it is not.

Both must be IMPLEMENTED or replaced by something real before either is claimed again.

---

## FIX AUDIT — v1's register against v2

| | verdict | note |
|---|---|---|
| P0-1 audio in all prompts | **PARTIAL** | `_SILENT` verbatim in A, B, C, D, F_load, E. **G still says only "no combustion is present at all"** — the register named G explicitly and the verbatim phrase is what does the work. |
| P0-2 split F | **LANDED** | `F_load` / `F_wake`; 11 clips, 247.5cr. |
| P0-3 F_wake stages the catch | **PARTIAL** | Prompt is right. But shot 10 has no in-point, so the catch lands ≈14.30s — 0.50s after the 13.80s cut, contradicting `HERO_SYNC = 0.00`. |
| P0-4 generate_audio | **NOT LANDED** | See above. Module-level flag, no per-source field, no reader. |
| P0-5 two-state + windows | **PARTIAL** | Prose landed. **No windows exist anywhere** — `SHOTS` is still a 4-tuple. |
| P0-6 Nev causes | **LANDED** | J: "HIS MOVEMENT COMES FIRST, the meter answers it." H: "HIS ACTION CAUSES THE DARKNESS." |
| P0-7 350PS retracted | **LANDED** | No number on any card. |
| P1-1 hero < 15s | **LANDED** | 13.80s. |
| P1-2 car named | **LANDED** | "CROWN. PULLING AWAY IN SILENCE". |
| P1-3 card 3 wording | **LANDED** | Matches `CONTENT["claim"]` exactly. |
| P1-4 rebuild shots 4–7 | **NOT LANDED** | Register said REBUILD; v2 CUT. The defect relocated to shots 11–14, where three of four changes are *engine-sound* states — invisible on a muted feed. |
| P1-5 hero headroom | **LANDED** | +12 over the gated floor, +30 over the near-silence. |
| P1-6 near-silence | **LANDED** | Shot 9, 12.60–13.80s, −24.0. |
| P1-7 bed_map | **PARTIAL** | Bed enters 1.80s while card 1 reads "…IN SILENCE" until 5.40s. 3.6s of music under the word silence — the exact complaint. |
| P1-8 blends / grid | **NOT LANDED** | Inert flag. See above. |
| P1-9 second hero | **LANDED** | Chime staged in H; one hero declared; −22.0. |
| P1-10 MIX block | **PARTIAL** | Present and correctly labelled unmeasured. Missing stereo width % and a reference track. |

---

## OPEN DEFECTS, RANKED

**A1 · The word HYBRID / HEV / EV appears on ZERO cards.** The mechanism that makes the
silence possible is never stated to a muted viewer. On a feed that autoplays silent, the
video is a car driving at sunset. **`CARDS[0]` → e.g. "CROWN. HYBRID. NO ENGINE YET"**.
This is the single highest-value line in the register.

**A2 · Card 2 spoils the hero.** `CARDS[1]` "HE ASKS. IT WAKES." starts at 12.60s (shot 9);
the wake is at 13.80s. The twist is announced 1.20s before it happens. **`first_shot` 9 → 10.**

**A3 · No shot windows exist, and E's second half is oversubscribed by 0.30s.** E's engine
half is ~2.5s and must serve shot 11 (1.20s) plus shot 14 (1.20s + 0.40s blend) = 2.80s.
Impossible. planqc check 21 reports "E 3.6/4.9" because it is blind to the two-state split —
**a green number on a check that cannot see the constraint.** Add a fifth field to `SHOTS`
and update `timeline()`, or move shot 14 to another source.

**A4 · The silence is mixed as FOREGROUND.** `FOLEY[0,1,4,8] = -6.0` — equal to shot 13
(engine hardened under load) and louder than shots 11, 12, 14. The generated doc will print
"Foreground (>=-6dB): shots [0, 1, 4, 8, 10, 13]". Worst is shot 8 on F_load, whose prompt
says *"the quiet immediately before something happens"*, at −6.0. **Set 0/1/4 ≈ −14, 8 ≈ −16
— which requires planqc 19's EVENT/PAYOFF ≥ −6 floor to be reconsidered for a pillar whose
events are made of absence.** Do not just override it; that floor is car-genre taste and this
is the first plan that contradicts it honestly.

**A5 · The hero has three attacks across 0.72s.** `IMPACT_AT` fires at 13.58s, the cut is at
13.80s, the prompt's catch is ≈14.30s. `HERO_SYNC` must be a MEASURED offset taken off the
ingested F_wake, not an asserted 0.00, and shot 10's in-point derived from it. F_wake is
already 4.20s committed of 5.00s — no slack for a late catch.

**A6 · The duck contradicts itself.** `bed_map["duck"]` says bed to −inf across shot 9;
`SOUND["duck"]` says sidechain −18dB starting 13.40s; `bed_map["hero"]` says nominal from
shot 10. Pick one owner. Also `planqc.py:1127` still emits *"Bed HARD-ducks"* into the doc.

**A7 · Shot 11's sound is not staged.** E's second half stages only *"receding… backing off"*;
shot 11 is "working now" at −8.0, the second-loudest engine gain, over material that only
decays. E is a three-state demand on a two-state prompt.

**A8 · Textless dead zone 5.40–12.60s** — 7.2s, 24% of the run, sitting on J0's commit
window. That is why J0 still vetoes at 5/10.

**A9 · Two linkage mislabels survive verbatim, and my docstring miscounts.** `LINKAGE[14]`
(arrival causes the quiet, not the reverse) and `LINKAGE[18]` (same event stated twice) are
the exact lines v1's register condemned. The docstring says "8 of 19 are CONSEQUENCE" and
names boundaries 9 and 18; planqc counts 9 and the agentive one is 17. Honest count ≈6.

**A10 · The CTA is enquire-tier on a Mode A hero film**, which file 01 forbids — "never sell
a car off an aspiration video". `CARDS[3]` names no seller and no channel.

**A11 · Zero local nouns in eleven prompts** (Sabah / Kota Kinabalu / Malaysia count = 0).
J4's respect trigger, free to close. The v1 geography error IS fixed — Kinabalu deleted.

**A12 · P2-1 blue-block motion floor** still open and still ~112cr of auto-rejects.

---

## RE-WALK CONDITIONS (all free, all pre-spend)

1. HYBRID on card 1; `CARDS[1]` moved to shot 10; a card inserted in the 5.40–12.60s gap.
2. Shot 5 (C repeat) replaced with a new image; shots 11–14 given one change that is visible
   with the sound off.
3. Fifth window field on `SHOTS`; shot 10's in-point at the catch; E's budget proved by
   arithmetic, not by check 21.
4. `BLEND_AS_OVERLAP` implemented in `planqc.py`/engine, or deleted and the timeline re-timed
   honestly. Same for `GENERATE_AUDIO` — implement the reader or remove the claim.
5. Verbatim "NO ENGINE, NO EXHAUST, NO COMBUSTION" into G.
6. FOLEY silence block dropped to ≈−14/−16, with planqc 19's floor re-examined rather than
   bypassed.
7. `LINKAGE[14]`/`[18]` retyped; docstring counts corrected.
8. P2-1 decided before ingest.

**Then, at probe:** shot A is now an audio test. `ebur128` for LUFS, `ffprobe` for channel
count (mono is the failure that created seat 19), and a combustion check at 60–250Hz. If A
comes back with an engine in it the concept dies at 22.5cr instead of 247.5cr.
