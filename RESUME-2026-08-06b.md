# RESUME — 2026-08-06b (session 5, remote/Cowork)

Read order: `CLAUDE.md` → `SYSTEM-MAP.md` → this file. Nothing here is optional.

> Session 4's `SYSTEM-MAP.md` worked exactly as intended: this session read the three
> files and was writing a gated plan inside the first third instead of re-deriving the
> architecture. Keep it current.

---

## WHERE WE ARE

**Balance: MEASURED 4,927.82 cr, plan `team`, 2026-08-06. ZERO credits spent this session.**

`plans/kundasang.py` — **NEW.** planqc **PASS 34/34**, judge panel **SHIP: YES**
(J0 8 · J2 7 · J4 8), cost **233.0cr total / 30.5cr probe**. Nothing generated.
**Plates `glc300` and `kundasang` are NOT BUILT** — 8cr for both.

`plans/crown.py` — still J4-vetoed from session 4, and **now also planqc-blocked** by
this session's ledger writes (see below). Untouched otherwise.

**NEXT ACTION:** his eye on the storyboard, then build the two plates (8cr), then
probe source A alone (22.5cr) and judge the tear timing before the other nine clips.

---

## THE NEW VIDEO — "NEV · ONE DAY IN KUNDASANG · GLC 300"

His title, his four picks, taken verbatim through the TITLE CONTRACT:

| decision | his pick | why it was a real fork |
|---|---|---|
| which GLC 300 | **X254 (2023+) 4Matic AMG Line** | the Supra-Final-Edition class of question |
| duration | **28.97s = 48 beats** | he said 30s; the measured band is 16-29s |
| bed | **Crystal-Water, 99.4 BPM, zero stretch** | KK already used to-the-moon |
| day arc | **dawn → dusk, open on the summit reveal** | 06:00-08:00 is the verified clear window |

**One correction made before it could poison anything:** 28.92s was quoted first,
computed at 97.5 BPM, and was stale the moment he picked a 99.4 BPM bed. The band-top
beat count at 99.4 is **48 beats = 28.9739s**. Caught and stated, not silently kept.

**Structure:** 20 shots, 16 burst (1.207s) + 4 med (2.415s), 10 sources, hard cuts only,
41.4 cuts/min against the profile's 40.3. Six light states, strictly monotonic.
19 boundaries, all **TYPED** carries — 5 of them consequences against a floor of 4.

**The market fact stays OFF the screen, deliberately.** carbase.my (fetched 2026-08-06):
the current Malaysian X254 range is GLC200 CKD RM329,888 and GLC350e 4Matic CKD
RM398,888; the GLC300 4Matic AMG Line was the CBU launch variant at RM429,888 and is
not in the new lineup. So a GLC 300 here in 2026 is used or recond — which is the
audience exactly. It lives in `CONTENT.verified` with its fetch date, because a market
status goes stale and **that is precisely what J4 vetoed crown for.**

---

## THE JUDGE EARNED ITS SEAT AGAIN — v1 VETOED, v2 SHIPS

planqc passed the v1 plan 34/34. The panel still found three things:

| seat | v1 | finding | fixed by |
|---|---|---|---|
| J0 | 6 PASS | the reveal did not RESOLVE inside 2.00s — it began at 0.00s, cut away, and landed at 2.41s, outside the gate. Nothing was LARGE at frame zero. | A reprompted so the tear finishes inside its own 1.21s window with the peak filling the upper half; B re-framed to shoot UP the road into depth so shot 1 continues the reveal instead of interrupting it |
| J2 | 6 PASS | 5 consequences existed but bunched at both ends — eight consecutive boundaries (8.45s–19.32s, a third of the film) where nothing happened because of anything. No friction anywhere in the day. | D's second window now carries the weather turning; boundary 9→10 is a real cause — the cloud closes, SO he pulls over. The golden hour is now light that was nearly lost. |
| J4 | 3 **FAIL, SOLO VETO** | card 3 "MINI NEW ZEALAND, THEY SAY" was verified by **nothing**. "THEY SAY" hedges attribution without sourcing it — the crown class, one day later. Independently: the card sat on shots 11-14 (parked car, railing, wheel) when the pasture is shots 7-9. | now quotes Daily Express Malaysia's published "Sabah's own little New Zealand" with the source in `CONTENT.verified`, **and moved onto shots 8-11** so it describes what is on screen |

v2: **J0 8 · J2 7 · J4 8 · SHIP YES.** The remaining risk moved from the plan to the
generator — "a fast tear finishing in one second" is a timing instruction a model may
not obey, and only the probe will show. That is a 22.5cr reroll of A alone, not a replan.

---

## HIS ASK, BUILT: `tools/lessonize.py` — the loop is now a MECHANISM

> *"every time each QC found a bug or an error it automatically fix and passed it to
> mastermind so next time on new generation the mastermind wont make this mistake
> again and essentially learn from it and improvise and improve"*

| half | status |
|---|---|
| **CAPTURE** | automatic. Every FAILING gate check and FAILING judge seat becomes a dated lesson in `ledgers/knowledge.json`. No human retyping, so nothing dies with a cleared chat. |
| **BLOCK** | automatic, and this is what makes it stick. Filing raises the topic COUNT, and **planqc 23 blocks any plan whose `LESSONS_ACK` is lower.** The next plan cannot pass until someone reads the new lessons and re-acks. |
| **FIX** | **REFUSED, permanently.** `mastermind_loop.py` already says it: fixing is judgement, and a loop that edited plans to clear its own gate would be optimising the CHECK instead of the FILM — the SMOOTH NUMBER trap from file 27, automated and unattended. |

**It caught its own first bug.** v1 read only the LATEST verdict; kundasang's J4 veto was
in run 1 and run 2 passed clean, so it filed **nothing** — the exact failure the tool
exists to prevent, in the tool itself. v2 reads every recorded run, deduped on a
normalised signature so re-running never inflates a count.

**Proof it works, end to end, today:** `general craft` **64 → 67**, and the very next
`planqc` run **BLOCKED `plans/kundasang.py`** — the plan that had just passed 34/34 —
until it re-acked at 67 and planned the three new lessons into its PREMORTEM. It now
passes again at 13 premortem risks.

### SIDE EFFECT HE MUST KNOW ABOUT
Raising the craft count to 67 **newly blocked `plans/crown.py`**, which was passing
35/35. Its only failing check is `23 lessons ack: acked 64, ledger holds 67`. That is
the mechanism doing what he asked, not a regression — but it is a state change he did
not ask for, so it is written down here. Crown was already J4-vetoed and ungenerated.
`kk.py` and `wrx.py` were **already** stale at 56 before this session touched anything,
and both carry other pre-existing failures (kk: 19 prose linkages, time runs backwards).

```
python3 tools/lessonize.py <project> --from-judge    file every FAILED seat, all runs
python3 tools/lessonize.py <project> --from-gate     run planqc, file every FAIL
python3 tools/lessonize.py --add "<topic>" "<CLASS: finding>" "<mitigation>"
python3 tools/lessonize.py --status                  counts + which plans are now BLOCKED
                                                     (--dry on any of them writes nothing)
```
Backs up `knowledge.json` to `_backup_lessonize/` before every write.

---

## MEASURED THIS SESSION, ZERO CREDITS

**KK v15, 18 detected shots** — the first real numbers this pillar has ever had:

| profile field | declared | measured on v15 | verdict |
|---|---|---|---|
| duration | 16–29s | 28.37s | in band |
| shot median | 1.13s (0.6–2.51) | 1.23s | in band |
| cuts/min | 40.3 ±20% | 38.1 | in band |
| blended | 0 — hard cuts only | 0 detected | holds |
| brightness_band | [35,200] **PROVISIONAL** | shots 56.6–133.1, frames 52.9–137.2 | no shot near either edge |
| motion_floor | 0.6 **PROVISIONAL** | shots 1.41–9.42 | 2.3× margin at worst |

**These do NOT flip the PROVISIONALs, and it matters why.** They come from the
delivered, graded, cropped cut. `clipqc` gates the **raw 5s clip's best window**,
pre-grade. Substituting one for the other is the OPEN-LOOP GAIN trap. **The raw clips
that could settle it are gone** — `projects/kk/clips/` holds `manifest.json` and an
empty `_to_delete`, every `kk_*.mp4` deleted. Filed as a craft lesson.

**Kundasang's ten raws are the replacement measurement. Do not delete them.**

---

## OPEN — HIS CALL, ALL FREE

- **`board.py` caps at 9 SOURCES.** Line 216 hardcodes `col_x = [L, L+760, L+1520]`,
  line 218 indexes `col_x[n // 3]` → `IndexError` on kundasang's 10. Nothing declares 9
  as a limit and planqc passes a 10-source plan happily. **FOREIGN LITERAL class.**
  One-line fix: `rows = ceil(n/3); cx = col_x[n // rows]; ry = gy + 34 + (n % rows)*46`.
  **PIPELINE FILE — his call, not an autonomous edit.** `tools/storyboard.py` renders
  this plan correctly today (20 shots, 7 plates, 13 red MISSING panels) and is what
  goes to the gate presentation, so nothing is blocked on it.
- **P2-1 motion floor** — still open from session 4, now with evidence: no shipped KK
  shot came within 2.3× of the 0.6 floor. Decide at kundasang's ingest, from raws.
- **23 references measured, ZERO READ.** `assets/refs/travel_vlog/` has 6 files and no
  `refsense.json` at all. Still the highest-value zero-credit work available.
- `plans/wrx.py` missing `hero_shot` (LATENT — wrx runs `edit_sfx=full`).
- `plans/i8.py` predates the plan format.

## MAP CORRECTION
`CLAUDE.md` says Playwright is "blocked in the Cowork sandbox — browser binaries 403."
**Stale.** Chromium is pre-installed in the remote container at `/opt/pw-browsers`, and
`playwright` imports fine there. `cards.py` can render remotely. Note that the Cowork
**Linux VM** reached by `device_bash` has ffmpeg 4.4.2 and cv2 but **no** playwright and
**no** pyflakes — three different machines, do not conflate them.

## STANDING RULES (unchanged)
- Never invent a number, path, credit cost or capability.
- **Claude cannot hear audio.** Every audio claim comes from a measurement.
- `device_bash` cannot delete — `cp`, then delete it yourself.
- Git on the Cowork bridge leaves stale `.git/*.lock`. `PUSH.bat` clears them.
- Chat is disposable, **the repo is the memory.**

## THE STANDING GAP
Still **zero posts, ever.** This session added a plan that ships, a judge round-trip
that caught a veto, and a mechanism that makes findings permanent. Not one of them can
tell you whether a video works. One posted video with a real 24-hour curve outranks
everything in this file.
