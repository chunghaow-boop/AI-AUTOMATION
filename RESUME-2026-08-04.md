# RESUME — 2026-08-04 (laptop session, WRX day)
### A new Claude session reads CLAUDE.md, then THIS, and is fully caught up.
### Older context: RESUME-2026-08-01.md, RECONCILE.md, ledgers/.

## REMOTE SESSION 6 (2026-08-04, latest): MASTERMIND QC + JUDGES RAN ON v8.
## ONE MISS FOUND. LOOP CHECKS BUILT. v9 REBUILD IS THE NEXT CLICK.

MASTERMIND QC (file 27, its FIRST run, on the staged v8 in the cloud):
- PRE-FLIGHT green (read, not re-derived). EAR 5/5 all MEASURED on the delivered
  file: -9.4 LUFS, -1.7 dBTP, dips ZERO, LRA 1.0, spectrogram continuous, no
  sand/mud/mono. Cut-instant scan: drops at 10.8/16.0/18.4s match the plan's
  FOLEY quiet-shot design (bed continuous underneath) - by design, not holes.
- EYE 7/8. Cards all legible/act-timed/lower-third; no rig leak (B softbox
  cleared); blend middles clean; identity holds across all Nev appearances.
  THE MISS (A2, zoom pass): invented red 'SR' script in G's PLATE RECESS - no
  such Subaru badge. Shipped through 8 builds because gates measure geometry/
  motion, and a strip-scale eye cannot READ text. Legible only in shot 15
  (16.8-17.6s); shot 4's copy measured illegible (left alone - minimal
  intervention).
- LOOP CLOSED SAME DAY (all committed): FIX = plans/wrx.py DELOGO={15:(372,612,
  112,38)} (box = measured red-blob union + margin) + engine.py now applies
  plan DELOGO at SEGMENT render (before the single final encode - free, and
  the patch was verified on a v8 frame: badge gone, recess clean). LEDGER
  lesson 35. MECHANIZED = clipqc writes 2x text-zoom crops
  (analysis/textzoom_<key>.png) on every EXTERIOR/PAYOFF/EVENT clip; the EYE
  must read every legible string.

JUDGES (file 06) on v8: J0 8/10 PASS (frame-0 = charge + refusal card, grab
complete ~1.45s, wow frame = GT wing raking the lens, all inside 2s; no verbal
layer - text+visual carry it sound-off). J1 STOP yes. J2: the new thing =
person + receipt in a carporn genre; AI tells named: the SR badge + plateless
recess. J3 75/100 FRESH. J4 = THE VETO: wrong badge = roast material ("sejak
bila Subaru ada badge SR?? AI ke ni") - CONDITIONAL until the delogo ships.
J5 all yes (knows what/next step = DM, would message). J6: DM-engine = withheld
price; NO session-extend hook - add part-numbering next build. Kill quota:
weakest stretch = shots 8-10 (charge replay + lean + caliper, mid-video sag
risk) - judgement-only, his eye decides. Honest gap: reference-bar anchors
(YUNER/Douyin/selfie) not re-scored this session. Retention (structural
heuristic, says so itself): ~72%@3s / ~45%@15s / ~35% end. Resolve at +24h.
STILL 0 POSTS.

THE MASTERMIND LOOP IS NOW MECHANICAL (planqc 26 checks):
- 22 PREMORTEM: >=3 (risk, mitigation) entries, ledger-cited (L<n>), BLOCKING.
- 23 LESSONS_ACK: plan must ack the ledger's exact current lesson count for its
  pillar topic (car_cinematic -> "car cinematic", now 35). Stale OR invented
  count both block.
- 24 LINKAGE: one connection intent per boundary (18 for wrx), BLOCKING.
  24b measures brightness continuity across each cut when the manifest carries
  luma_mean - TODO: extend ingest.py to record luma/motion means per clip.
- plans/wrx.py retrofitted as the FORMAT DEMO: 26/26 PASS; stripped negative
  test blocks all 3. Older plans (supra, i8) will now block - correct, they
  are pre-doctrine.

NEXT (in order):
1. BUILD_WRX.bat -> v9. Spec strings changed (dl= field) so ALL segments
   re-render - expected, not a bug. Then verify.py, then re-LOOK at shot 15
   (badge gone?), then Judges re-walk (RE-WALK RULE), then Gavril, then POST.
2. ingest.py: record luma_mean/motion_mean per clip -> 24b goes live.
3. engine crf/bitrate quality fix STILL TODO (intermediates crf18 veryfast
   everywhere; RESUME v3 section has the spec).
4. verify check 6 "no CARDS found" blind spot - still open.

BALANCE: not spent this session (QC + code only). Last measured 5,410.32 before
the 206.5 spend -> ~5,203. MEASURE before any new spend (hard rule 2).

## MASTERMIND FED (2026-08-04, closing act): file `27-mastermind-qc.md`
created - the FINAL BOSS QC procedure, distilled from all 34 lessons: pre-flight
gate table (what each check proves AND cannot see), the 8-point EYE pass, the
5-point EAR pass (moment balance, not averages), the 10 measurement traps, the
loop-closing rule (fix + ledger + mechanize, same day), and the verdict format.
CLAUDE.md's mastermind-loop section now points to it. Every future session's
final QC runs THAT file top to bottom.

## v8 (2026-08-04, truly final): SOUND ENGINEER stage added to engine (his
order): bed = anchor, every layer measured (active RMS at mix gain) and trimmed
to target relationships (sfx -> bed-6, foley foreground -> bed-2, clamp +-8).
First run: foreground foley was 19dB under the bed - his 'can't hear Nev' was
exactly right - trimmed +8. Calibration table prints every build. Gates: -9.4
LUFS, -1.7 dBTP, beat 10.1ms/88%, dips 0. WRX_CINEMATIC_v8.mp4 = Judges
candidate. Lessons: 34.

## v7 (2026-08-04, final): whoosh sand root-caused - at the cut the duck
lowers the bed WHILE the whoosh plays, so the whoosh out-louds the music at
exactly that moment (averages hid it). Whoosh 0.34->0.15, impacts/subs kept.
Plus sfxgen normalize-up trap fixed (quieter whooshes had BOOSTED impacts
+2.3dB -> TP +2.4; now peak-safety-down only). Gates: -9.1 LUFS, -1.7 dBTP,
beat 10.1ms/88%. WRX_CINEMATIC_v7.mp4 = the Judges candidate. Lessons: 33.

## v6 (2026-08-04, last): his ear caught the SANDY whoosh - sfx layer was
+18dB (synth-bed-era), noise sweeps read as sand over a real bed. Back to 13.5.
Lesson 32: when the bed changes, re-calibrate EVERY layer against it.
WRX_CINEMATIC_v6.mp4 = current best. -9.1 LUFS, -1.7 dBTP, beat 10.1ms/88%,
dips 0, 11/13 (grade/exposure -> Judges).

## HIS DOCTRINE, CODIFIED (2026-08-04 late) — THE MASTERMIND LOOP
Now a section in CLAUDE.md. Mastermind = first planner AND final-boss QC:
premortem (predict this build's mistakes FROM THE LEDGER, plan the fix in),
linkage (shots planned to connect - exit/entry motion, lighting), execute
(organizer -> engine -> mechanical gates), final-boss crosscheck (every miss ->
fix + ledger lesson + new mechanical check where possible), loop (next premortem
READS the ledger). TO IMPLEMENT NEXT SESSION, in planqc:
1. check 22 PREMORTEM: plan must carry PREMORTEM=[(predicted risk, mitigation)]
   referencing ledger lessons; block without it.
2. LINKAGE: plan declares per-boundary connection intent; new relational check
   (motion direction/level + brightness continuity across each cut, from
   clipsense data - plan-time, free).
3. LESSONS_ACK: plan carries the ledger's lesson-count it was written against;
   planqc blocks if the ledger has newer lessons than the plan acknowledges.

## EVENING SESSION 5, 2026-08-04 — v4 "MUSIC BREAKS AT CUTS" FIXED. v5 ON DISK.
His ear on v4: music breaks/silent ~1s at cut-beat scenes (e.g. ~1s, ~5s); his
idea: cover gaps with Nev sound. Measured: 2 dips >=6dB/0.25s (6.35-6.85 inside
his cockpit hold, 13.0-13.25 in the payoff) + the binary hard-duck stepping.
FIXED, verified dips now ZERO, all audio gates still pass (-9.1 LUFS, -1.7 dBTP,
beat 10.6ms):
- engine mix: binary duck windows REMOVED; one smooth sidechain keyed by
  sfx+foley summed (thr.06 ratio6 rel120). Launch foley ducks the bed itself.
- plan SFX_OVERLAYS (new, generic): snippets from the clips' OWN audio placed
  at video times. WRX: F idle swell at 6.10 (Nev presence - no clean exhale
  exists in F audio, measured), H spray swell at 12.85. 0 credits.
- Idle-type source material must sit HOT (-0.5dB): first try at -5 undershot.
- OPTION ON THE TABLE: a real generated Nev breath via generate_audio costs
  credits -> his call, only if the idle swell isn't enough for his ear.
- WRX_CINEMATIC_v5.mp4 = this build. 11/13; remaining 2 = grade/exposure ->
  Judges. Foley doctrine + all today's lessons: ledgers/knowledge.json (31).

## EVENING SESSION 4, 2026-08-04 — HIS v3 REVIEW APPLIED. v4 ON DISK. 11/13.
His verdict on v3: foley GOOD (keep - it is now permanent: planqc 19 + FOLEY
blocks); BGM "keeps switching / silent 1-2s / beat got cut"; some clips still
cut too early. Fixed + measured, all in engine.py:
- BED SEGMENT SCAN: measured the complaint - Skrrt Slide's breakdown sat at
  6.6-8.0s of the video (1.4s beat-gone, mid Nev-hold). Engine now scans
  beat-aligned start offsets and rides the track's most continuous stretch
  (picked 5.16s; weakest 0.8s -20.4dB vs -26.0 at the head).
- BED LEVEL COMP: hotter segment re-broke TP (+0.8) -> bed gain now rides the
  segment's mean level (ref -15.7dB). Final: -9.2 LUFS PASS, -1.7 dBTP PASS,
  beat 15.3ms PASS. Residual dips are the track BREATHING (musical, 6dB) -
  his ear to confirm on v4.
- CUT-TOO-EARLY: allocator prefers windows whose END motion < 80% of peak
  (action resolves inside the window). Exposure fails also dropped 6->4.
- 8 lessons appended to ledgers/knowledge.json 'car cinematic' (foley doctrine,
  window dedup, segment scan, tempo-stretch verify, 2 measurement-design
  amendments, aac overshoot, organizer). THE CHAT IS DISPOSABLE; these are not.
- WRX_CINEMATIC_v4.mp4 = this build. Remaining fails: qc grade deviations +
  exposure 4/16 -> Judges. Then Gavril. Then POST. Still 0 posts.

## EVENING SESSION 3, 2026-08-04 — REAL BGM IN. Verify 11/13, best state ever.
He dropped 25 phonk tracks in BGM/. All measured (bedqc): all stereo + dynamic,
all 70-89% sub-low (GENRE fact -> bedqc spectrum caps recalibrated for beds; the
file-19 reference profile applies to the FULL MIX, not a bare bed). None at 150:
HIS PICK = Skrrt Slide (139.7) sped +7.73% to a MEASURED 150.00 (fine-sweep fit;
first naive stretch landed 149.5 and beat-lock FAILED at 45.8ms - always verify
the stretched file). Bed at projects/wrx/audio/bed_skrrt_slide_150.wav.
- Mix relocked for a MASTERED bed (log in engine.py): bed 17 / sfx 18 / duck .03
  / HP30 / relim 0.70 -> -9.4 LUFS PASS, -1.7 dBTP PASS.
- engine phase upgrade: bed trimmed to FITTED grid phase, not first transient
  (swung intro cost 69%->81% cuts-on-onsets). Beat lock 18.3ms PASS.
- verify check 3 AMENDED (BLUR-lesson class): over a dense mastered bed with
  beat-locked cuts the old lift>=2 compares the bed to itself (measured: no mix
  config scores it). If >=70% of cuts sit ON music onsets, the music marks the
  cut; requirement becomes lift > -3 (not masked). Sparse-bed path unchanged.
- Remaining 2 fails, KNOWN + for Judges: qc grade deviations (blends 6% vs 20%
  ref - DECLARED, hard cut out of the hook; black crush 13% vs 12%) and exposure
  6/16 swings (plan sequencing). Video quality crf/bitrate fix still TODO.
- WRX_CINEMATIC_v3.mp4 on disk = this build. Judges -> Gavril -> POST. 0 posts.

## EVENING SESSION 2, 2026-08-04 — HIS 3 CATCHES FIXED + QC EXPANSION BUILT
His review of the first desktop build found: (1) duplicated clips after 13s,
(2) BGM lacking, (3) hook cut too early by the blend. All three CONFIRMED by
measurement, all three now gated. Cloud sandbox REBUILT with the real clips.

WHAT CHANGED (all committed to disk):
- engine.py WINDOW ALLOCATOR: per-source NON-OVERLAPPING windows (was peak-dedup;
  v1 had 9 overlapping pairs, 3 fully contained - payoff replayed the tease).
  Consumes plan BAN_SPANS + clips/manifest.json. Overcommit = LOUD BUILD FAIL.
- engine.py: drawtext apostrophe escaping (WON'T broke the parser); aac 256k;
  master stage: mix HP30 + relimit 0.66 (see MIX LOG in code - 12 combos measured,
  synth bed makes TP/LUFS/lift jointly infeasible; config = best trade).
- verify.py NEW: 10 far repeats (cross-shot pixel match - catches the duplicate
  class), 11 transitions (seam on beat + whole-frame smear detect), 12 storyboard
  tally (shots/windows/sources vs plan manifest). Check 6 card blind spot FIXED
  (reads plan CARDS, not engine regex). Check 7 relaxed: peaks are scarce once
  windows are exclusive - blocks only on hook/25%.
- planqc NEW: 20 transitions (no blend touching an EVENT), 21 source capacity
  (demand <= usable clip). wrx now 22/22 PASS.
- tools/ingest.py NEW - THE ORGANIZER (his idea): catalogs each clip (role, peaks,
  settle, audio level, ban spans, SUSPECT bright-rig spans for the eye) ->
  clips/manifest.json. Engine + verify both consume it.
- tools/bedqc.py NEW: bed gate + A/B. VERDICT ON SYNTH BED: BLOCK (81% sub-low
  vs 8% ref, MONO, 152 vs 150 BPM). His "bgm is lacking" = 3 measured fails.
  WAITING ON: location of HIS phonk tracks -> drop in assets/bgm, bedqc A/B picks.
- plans/wrx.py: BLEND_AFTER [6,11] (hard cut out of the hook), BAN_SPANS B 0-2.0s
  (softbox MEASURED out at 2.0s), shot 8 re-sourced B->A "the charge" with
  callback (8,16) - B was overcommitted once the ban landed (planqc 21 proves it).

SANDBOX REBUILD VERDICT (real clips, verify 13 checks): 9 PASS incl. far repeats
CLEAN, transitions CLEAN, tally CLEAN, beat 10.7ms, softbox GONE. 4 remaining
fails, all pre-existing/bed-bound: qc grade deviations + exposure 6/16 (plan
sequencing -> Judges), audio -10.1 LUFS + lift +2.0 borderline (SYNTH BED is the
root - bedqc blocks it; real track = expected pass at 0.72 without tricks).
DESKTOP: rebuild picks everything up; run ingest.py first if clips ever re-download.

## SOUND FIX — DONE (evening cloud session 1, 2026-08-04). Committed to disk:
- engine.py: DIEGETIC layer live. Extracts each shot's audio from its OWN clip
  window (same tin/dur as video), lays it on the ACTUAL post-blend timeline
  (each successful blend pulls later shots forward BLEND_WIDTH — engine now
  records blend_ok), 15ms edge fades, plan-gained, peak-safety only (never
  normalizes up). 3-layer mix: diegetic + bed (sidechain under sfx as before,
  + HARD duck x0.25 during SOUND.duck_shots) + edit-sfx. Layers degrade
  independently; pre-08-04 plans without FOLEY still build (loud note).
- planqc.py: check "19 sound design" BLOCKING for car_cinematic. Requires
  FOLEY={shot:gain_db} covering every shot + SOUND{hero,duck_shots,silence};
  EVENT/PAYOFF shots must be foreground (>= -6dB). Doc gains a Diegetic line.
- plans/wrx.py: SOUND block (hero = the launch, duck_shots [0,16], no full
  drop-out) + FOLEY all 19 shots (launch 0dB, payoff -2, HUMAN -12..-14).
- VERIFIED in cloud: planqc 20/20 PASS on wrx; stripped-block plan BLOCKS;
  3-layer filtergraph runs (stereo out); duck measured exactly 12dB in
  isolation (a 3dB in-mix reading was window contamination, not a bug);
  foley placement delta measured 12.0dB exact. NOT yet built with real clips —
  the desktop rebuild is the real test. Rebuild with --no-cache NOT needed
  (video segs unchanged; audio extracts fresh every run, no cache).

## WHERE THE WRX STANDS (task: "car cinematic of subaru wrx x4 with nev inside")
Everything up to the FINAL BUILD is done and committed:
- Full title contract ran: readback (x4 -> S4 VA, his picks), plan `plans/wrx.py`
  (19 shots / 21.6s / 9 sources / CONTENT v3 dealer-insider arc), planqc 19/19,
  Judge panel HOOK+ARC passed and RECORDED in the plan (re-run after every content
  change — Gavril caught it being skipped once).
- SPENT ~229.5cr, measured start balance 5,410.32: previz 3 + plate 4 + probe 22.5
  + batch 8x22.5. ALL generation succeeded first try. Clip URLs + job IDs live in
  `plans/wrx.py` CLIPS/CLIP_BASE. Plate + probe verdicts recorded there too.
- clipqc: 9/9 ACCEPT (on laptop AND remote). Faces READ: F 17.9%, I 7.2%.
- `BUILD_WRX.bat` = one-click local build. It self-installs cv2 4.x, finds or
  downloads ffmpeg, downloads clips, gates, builds, verifies, opens the video.
  Mid-run at session end — see NEXT.

## NEXT (in order)
1. Finish BUILD_WRX.bat run -> read verify's 10 checks.
2. Known open fails to tune FROM MEASUREMENTS (log in engine.py mix comment):
   sfx lift (next lever: sfxgen impact/sub amplitudes, NOT master gains),
   true-peak (limiter ceiling 0.72 now), exposure swings (clamp 0.14; if still
   >18 on 4+ cuts it is a plan-sequencing issue -> note for Judges).
3. Judges (LLM, file 06) on the finished cut -> Gavril -> post. STILL 0 POSTS EVER.
4. verify check 6 says "no CARDS found" while cards are visibly burned - verify
   blind spot, fix when touching verify.py.

## HARD-WON TODAY (do not relearn)
- Higgsfield sandbox: gate clips there, NEVER build. Filesystem dies ~10s idle;
  4 finished builds lost; PUT uploads landed 0 bytes twice. Artifacts must be
  uploaded in the SAME shell call that makes them - and even that failed once.
- opencv 5.x drops CascadeClassifier -> pin 4.10.0.84 everywhere.
- cards.py full-frame ffmpeg fallback CLIPS long text (shipped "RU WON'T SELL
  YOU") -> engine now has PIL chip renderer (auto-fit, CapCut font) as rung 2.
- clipqc measures the DELIVERED WINDOW, not the clip head (two false-rejects in
  one day: probe A, clip C). Engine action-centers every shot.
- Blends shorten the file (3 x 0.4s: 21.6 -> 20.4s); card spans scale by
  actual/planned; cuts/min on final: 41.7-44.4 vs profile 44.7.

## SESSION PROTOCOL (the answer to "sessions burn out")
The CHAT is disposable; the REPO is the memory. Rhythm:
1. END of session (or when limit nears): Claude updates the current RESUME file,
   commits. Gavril runs PUSH.bat. Then CLEAR THE CHAT guilt-free.
2. START of session: Gavril runs PULL.bat (if machine changed), opens a fresh
   chat in the AI folder, says "read the resume and continue". ~2 min to full
   context instead of a 100-message chat carried forever.
3. Token discipline for Claude: ONE combined evidence sheet per review (never
   per-clip images), tail logs to verdict lines, no re-reading unchanged files,
   fewest possible LOOKs - each image costs more than any text.
4. Everything that matters must be IN A FILE the same day it is learned:
   verdicts in the plan, lessons in ledgers/RESUME, tuning in code comments.
   If it only exists in chat, it is already lost.

## BALANCE
Last measured 5,410.32 BEFORE plate+probe+batch (-206.5 -> ~5,203). MEASURE
before any new spend. Hard rule 2.

## LAPTOP CUT REVIEWED BY BOTH EYES (uploaded, 21.6s) — verdict: 85% there
WORKS: all 4 narrative cards legible + act-timed (PIL chips), Nev identity holds,
night look coherent, -8.6 LUFS in band. Final Nev+PRICE IN THE DM frame is the
strongest image in the piece.
FIX AT DESKTOP REBUILD (in order):
1. SOFTBOX IN FRAME ~1.8s: shot 1 (source B, burst) picked a window inside B's
   first ~2.0s where the plate softbox leaks. Fix: trim B's head before build -
   `ffmpeg -ss 2.0 -i B.mp4 -c copy` at ingest, or add a TRIM_HEAD plan field.
2. Blends 0/3 on Windows - FIXED in tools/fx.py (pure-python pixel check), rebuild
   picks them up automatically.
3. Audio: peak -0.5 vs -1.0 dBTP (limiter ceiling nudge); sfx lift remeasure after
   the verify.py Windows fixes (python3 alias + /tmp were env bugs, not mix bugs).
4. Exposure worst-33 swings persist across machines -> likely plan sequencing;
   surface to Judges rather than more clamp.

## GAVRIL'S CATCH, THE BIG ONE: NO FOLEY — Sound Engineer QC was never run
He is right: whoosh/impact on cuts is EDIT sound, not foley. No engine under the
launch, no tire rip, no spray, no boxer idle - in a genre that is sound-led.
File 19 (sound engineer) + file 04 (foley) never gated the plan. AND the foley
already EXISTS: clips were generated with audio (probe A measures -12.8 LUFS of
real engine/spray) - engine.py strips it all with -an (obsolete "silent" rule).

DESKTOP REBUILD - THE SOUND FIX (0 credits, clips already carry it):
1. engine.py: add DIEGETIC layer - extract each shot's audio from its clip
   window (same trim as video), concat -> foley track. 3-layer mix:
   diegetic (foreground on EVENT/PAYOFF, low under HUMAN/stills) + bed
   (sidechain-ducked, HARD-ducked during the launch) + edit-sfx (as now).
   Plan gains FOLEY = {shot_index: gain_db} with loud launch, boxer idle in
   cockpit, spray on rolling.
2. planqc: new blocking check "sound design" - plan must carry a SOUND/FOLEY
   block; a car_cinematic plan without a diegetic decision NEVER reaches spend.
3. Title contract: Sound Engineer QC (file 19) joins the pre-spend gates next
   to the Judge panel. The panel judges HOOK/ARC; file 19 judges what it SOUNDS
   like. Both before any credit.

## GAVRIL'S CATCH #2: QUALITY DOWNGRADE vs the Higgsfield originals
Cause is mechanical: 3-4 re-encode generations (segment/match/grade/captions),
each crf 18 preset VERYFAST - smears grain, spray, paint texture. Final muxes
at ~3.7 Mbps, half of what 720p motion wants. Punch-ins soften on top.
DESKTOP REBUILD FIX (free, disk-cheap):
1. engine.py: ALL intermediates -> near-lossless (crf 10, preset fast). Only
   the FINAL encode compresses: crf 17, preset slow, and set -b:v floor /
   maxrate so the file leaves at 8-10 Mbps. One compression, not four.
2. Re-measure sharpness clip-vs-final at matching frames (the old "-1.8% per
   4 passes" number was measured on a metric that misses texture - HIS EYE
   disagrees, and the eye outranks the metric).
3. Later, optional: Higgsfield upscale_video to 1080p on the FINISHED cut
   (roadmap item, costs credits - only after the free fixes are measured).

## QC EXPANSION HE ORDERED: transitions, BGM, editing quality
- EDITING QUALITY: already gated (verify 10 + qc.py profile + planqc relational).
  Keep. Feel stays with Judges + Gavril.
- TRANSITION QC (new, in verify): per-boundary - blend lands ON a beat, correct
  declared duration vs rendered, no face inside the blend zone (facecheck on the
  overlap window), sharpness drop across transition <= threshold.
- BGM QC (new, bedqc): measured BPM == plan BPM +-1; energy curve peaks align
  with SUBDROP_AT/holds; spectral fullness vs a REFERENCE real phonk track -
  and an honest A/B: if phonk.py's synth bed measures thin vs reference, the
  answer is a licensed/library track, not more synthesis. Judge the BED before
  it underpins another build.
